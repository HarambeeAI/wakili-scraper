---
phase: 15-personal-assistant-operational-agents
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - worrylesssuperagent/langgraph-server/src/tools/pr/media-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/pr/sentiment-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/pr/index.ts
  - worrylesssuperagent/langgraph-server/src/tools/pr/media-tools.test.ts
  - worrylesssuperagent/langgraph-server/src/tools/procurement/supplier-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/procurement/po-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/procurement/index.ts
  - worrylesssuperagent/langgraph-server/src/tools/procurement/supplier-tools.test.ts
  - worrylesssuperagent/langgraph-server/src/tools/data-analyst/query-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/data-analyst/analysis-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/data-analyst/index.ts
  - worrylesssuperagent/langgraph-server/src/tools/data-analyst/query-tools.test.ts
  - worrylesssuperagent/langgraph-server/src/tools/operations/project-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/operations/process-tools.ts
  - worrylesssuperagent/langgraph-server/src/tools/operations/index.ts
  - worrylesssuperagent/langgraph-server/src/tools/operations/project-tools.test.ts
autonomous: true
requirements: [OPS-04, OPS-05, OPS-06, OPS-07]

must_haves:
  truths:
    - "PR can draft press releases, monitor media via Firecrawl, track coverage, and analyze sentiment"
    - "Procurement can search suppliers via Firecrawl, compare quotes, create POs with HITL, and score vendors"
    - "Data Analyst can run pre-defined cross-functional queries and detect anomalies via z-score"
    - "Operations can create projects with milestones, analyze bottlenecks, and draft SOPs"
    - "All 4 agent tool sets are barrel-exported from their respective index files"
  artifacts:
    - path: "worrylesssuperagent/langgraph-server/src/tools/pr/media-tools.ts"
      provides: "draftPressRelease, monitorMedia, trackCoverage"
      exports: ["draftPressRelease", "monitorMedia", "trackCoverage"]
    - path: "worrylesssuperagent/langgraph-server/src/tools/pr/sentiment-tools.ts"
      provides: "analyzeSentiment"
      exports: ["analyzeSentiment"]
    - path: "worrylesssuperagent/langgraph-server/src/tools/procurement/supplier-tools.ts"
      provides: "searchSuppliers, compareQuotes, scoreVendor"
      exports: ["searchSuppliers", "compareQuotes", "scoreVendor"]
    - path: "worrylesssuperagent/langgraph-server/src/tools/procurement/po-tools.ts"
      provides: "createPurchaseOrder (HITL)"
      exports: ["createPurchaseOrder"]
    - path: "worrylesssuperagent/langgraph-server/src/tools/data-analyst/query-tools.ts"
      provides: "crossFunctionalQuery, kpiAggregation"
      exports: ["crossFunctionalQuery", "kpiAggregation"]
    - path: "worrylesssuperagent/langgraph-server/src/tools/data-analyst/analysis-tools.ts"
      provides: "statisticalAnalysis, anomalyDetection, generateChart"
      exports: ["statisticalAnalysis", "anomalyDetection", "generateChart"]
    - path: "worrylesssuperagent/langgraph-server/src/tools/operations/project-tools.ts"
      provides: "createProject, addMilestone, listProjects, trackMilestones, analyzeBottlenecks"
      exports: ["createProject", "addMilestone", "listProjects", "trackMilestones", "analyzeBottlenecks"]
    - path: "worrylesssuperagent/langgraph-server/src/tools/operations/process-tools.ts"
      provides: "draftSOP"
      exports: ["draftSOP"]
  key_links:
    - from: "src/tools/data-analyst/query-tools.ts"
      to: "QUERY_TEMPLATES"
      via: "Pre-defined safe SQL templates (no LLM-generated SQL)"
      pattern: "QUERY_TEMPLATES"
    - from: "src/tools/procurement/po-tools.ts"
      to: "src/hitl/interrupt-handler.ts"
      via: "interruptForApproval for create_purchase_order"
      pattern: "interruptForApproval"
    - from: "src/tools/operations/project-tools.ts"
      to: "public.projects"
      via: "SQL CRUD"
      pattern: "public.projects"
---

<objective>
Build tools for PR (OPS-04), Procurement (OPS-05), Data Analyst (OPS-06), and Operations (OPS-07): press releases + media monitoring, supplier search + PO with HITL, cross-functional queries + anomaly detection, and project management + SOP drafting.

Purpose: Completes the remaining 4 operational agents' tool sets. After this plan, all 7 COO-routed agents have real tool execution.
Output: 8 tool files, 4 barrel indexes, 4 test files.
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-personal-assistant-operational-agents/15-RESEARCH.md
@.planning/phases/15-personal-assistant-operational-agents/15-UI-SPEC.md
@.planning/phases/15-personal-assistant-operational-agents/15-01-SUMMARY.md

<interfaces>
From src/tools/shared/db.ts:
```typescript
export function getPool(): pg.Pool;
```

From src/llm/client.ts:
```typescript
export async function callLLM(messages, options): Promise<{ content: string; tokensUsed: number }>;
export async function callLLMWithStructuredOutput<T>(messages, schemaDescription, options): Promise<{ data: T; tokensUsed: number }>;
```

From src/hitl/interrupt-handler.ts:
```typescript
export function interruptForApproval(interruptPayload: InterruptPayload): ApprovalDecision;
// HIGH_RISK_ACTIONS already includes "create_purchase_order"
```

From src/tools/marketer/research-tools.ts (Firecrawl helper pattern):
```typescript
// firecrawlSearch(query) pattern used for web search
```

From src/tools/accountant/anomaly-tools.ts (z-score pattern):
```typescript
// Z-score calculation: (value - mean) / stdDev, flag if |z| > 2
```

Type contracts from Plan 01:
- src/tools/pr/types.ts: PRClassification, PressCoverageRow, MediaMention, SentimentAnalysis
- src/tools/procurement/types.ts: ProcClassification, SupplierResult, QuoteComparison, PurchaseOrderInput, VendorScore
- src/tools/data-analyst/types.ts: DAClassification, QueryResult, ChartData, AnomalyResult, KPISummary
- src/tools/operations/types.ts: OpsClassification, ProjectRow, MilestoneRow, BottleneckAnalysis, SOPDocument, ProjectStatusSummary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PR tools + Procurement tools (with HITL PO) + tests + barrels</name>
  <files>
    worrylesssuperagent/langgraph-server/src/tools/pr/media-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/pr/sentiment-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/pr/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/pr/media-tools.test.ts,
    worrylesssuperagent/langgraph-server/src/tools/procurement/supplier-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/procurement/po-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/procurement/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/procurement/supplier-tools.test.ts
  </files>
  <read_first>
    worrylesssuperagent/langgraph-server/src/tools/pr/types.ts,
    worrylesssuperagent/langgraph-server/src/tools/procurement/types.ts,
    worrylesssuperagent/langgraph-server/src/tools/shared/db.ts,
    worrylesssuperagent/langgraph-server/src/tools/marketer/research-tools.ts,
    worrylesssuperagent/langgraph-server/src/hitl/interrupt-handler.ts,
    worrylesssuperagent/langgraph-server/src/llm/client.ts,
    .planning/phases/15-personal-assistant-operational-agents/15-UI-SPEC.md
  </read_first>
  <action>
**PR TOOLS (OPS-04):**

**Create `src/tools/pr/media-tools.ts`** with 3 functions:

1. **`draftPressRelease(userId: string, topic: string, keyPoints: string[]): Promise<{ pressRelease: string; message: string }>`**
   - Call `callLLM` with system prompt: "Draft a professional press release. Include headline, dateline, body (inverted pyramid), quotes placeholder, and boilerplate."
   - Message from UI-SPEC: `"Press release drafted: \"{title}\""`

2. **`monitorMedia(userId: string, businessName: string): Promise<{ mentions: MediaMention[]; message: string }>`**
   - Use Firecrawl search (same pattern as marketer's `monitorBrandMentions`): extract `firecrawlSearch` helper or reimplement the fetch to `https://api.firecrawl.dev/v1/search` with `FIRECRAWL_API_KEY` from env
   - Call `callLLMWithStructuredOutput` to classify sentiment of each result
   - Empty message from UI-SPEC: `"No media mentions found for \"{query}\"."`
   - Result message: `"Found {mentionCount} mention(s) across {sourceCount} source(s). Sentiment: {positiveCount} positive, {neutralCount} neutral, {negativeCount} negative."`

3. **`trackCoverage(userId: string, publication: string, title: string, url?: string, journalist?: string, reach?: number, sentiment?: string): Promise<{ coverageId: string; message: string }>`**
   - INSERT into public.press_coverage RETURNING id
   - Message from UI-SPEC: `"Coverage tracked: \"{title}\" in {publication}"`

**Create `src/tools/pr/sentiment-tools.ts`** with 1 function:

4. **`analyzeSentiment(userId: string): Promise<SentimentAnalysis>`**
   - SELECT sentiment, COUNT(*) FROM press_coverage WHERE user_id=$1 GROUP BY sentiment
   - Calculate overall sentiment and score (-1 to 1 range)
   - Message from UI-SPEC: `"Overall sentiment: {overallSentiment} ({sentimentScore})"`

**Create `src/tools/pr/index.ts`** barrel:
```typescript
export { draftPressRelease, monitorMedia, trackCoverage } from "./media-tools.js";
export { analyzeSentiment } from "./sentiment-tools.js";
export type { PRClassification, PressCoverageRow, MediaMention, SentimentAnalysis } from "./types.js";
```

**Create `src/tools/pr/media-tools.test.ts`**: Mock llm/client, mock fetch (for Firecrawl), mock db. Tests: draftPressRelease calls LLM, monitorMedia searches and classifies sentiment, trackCoverage inserts row.

---

**PROCUREMENT TOOLS (OPS-05):**

**Create `src/tools/procurement/supplier-tools.ts`** with 3 functions:

1. **`searchSuppliers(userId: string, query: string): Promise<{ suppliers: SupplierResult[]; message: string }>`**
   - Use Firecrawl search for supplier discovery (same pattern as PR media monitoring)
   - Call `callLLMWithStructuredOutput` to extract structured supplier info from search results
   - Empty message from UI-SPEC: `"No suppliers found for \"{query}\"."`
   - Result message: `"Found {count} potential supplier(s) for \"{query}\"."`

2. **`compareQuotes(userId: string, suppliers: Array<{ name: string; price: number; terms: string; quality: number; leadTime: string }>): Promise<QuoteComparison>`**
   - Score each supplier: normalize price (lower=better), quality (higher=better), parse leadTime
   - Total score = weighted average (price 40%, quality 30%, leadTime 30%)
   - Message from UI-SPEC: `"Quote comparison: {supplierCount} supplier(s) compared. Best value: {bestSupplier} at {bestPrice}."`

3. **`scoreVendor(userId: string, supplierName: string): Promise<VendorScore>`**
   - Query agent_audit_log or procurement history for past interactions (if available)
   - Calculate reliability, price, quality scores 0-100
   - Message from UI-SPEC: `"Vendor score for {supplierName}: {score}/100 (Reliability: {reliability}, Price: {price}, Quality: {quality})"`

**Create `src/tools/procurement/po-tools.ts`** with 1 function:

4. **`createPurchaseOrder(userId: string, agentType: string, input: PurchaseOrderInput): Promise<{ created: boolean; poId?: string; message: string }>`**
   - Call `interruptForApproval({ action: "create_purchase_order", agentType, description: \`Create purchase order: ${input.quantity}x "${input.itemDescription}" from ${input.supplier} for ${input.totalAmount}\`, payload: input })`
   - If not approved, return `{ created: false, message: "Purchase order creation cancelled by user." }`
   - If approved, INSERT into agent_tasks or a dedicated tracking structure. Since no purchase_orders table exists, store as an agent_asset with asset_type='purchase_order'.
   - Message from UI-SPEC: `"Purchase order #{poId} created: {totalAmount} to {supplier}"`

**Create `src/tools/procurement/index.ts`** barrel:
```typescript
export { searchSuppliers, compareQuotes, scoreVendor } from "./supplier-tools.js";
export { createPurchaseOrder } from "./po-tools.js";
export type { ProcClassification, SupplierResult, QuoteComparison, PurchaseOrderInput, VendorScore } from "./types.js";
```

**Create `src/tools/procurement/supplier-tools.test.ts`**: Mock fetch (Firecrawl), llm/client, db, hitl. Tests: searchSuppliers calls Firecrawl, compareQuotes scores correctly, createPurchaseOrder calls interruptForApproval.
  </action>
  <verify>
    <automated>cd worrylesssuperagent/langgraph-server && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/tools/pr/media-tools.test.ts src/tools/procurement/supplier-tools.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/tools/pr/media-tools.ts contains "export async function draftPressRelease"
    - src/tools/pr/media-tools.ts contains "export async function monitorMedia"
    - src/tools/pr/media-tools.ts contains "export async function trackCoverage"
    - src/tools/pr/media-tools.ts contains "press_coverage"
    - src/tools/pr/sentiment-tools.ts contains "export async function analyzeSentiment"
    - src/tools/pr/index.ts contains "export { draftPressRelease"
    - src/tools/procurement/supplier-tools.ts contains "export async function searchSuppliers"
    - src/tools/procurement/supplier-tools.ts contains "export async function compareQuotes"
    - src/tools/procurement/po-tools.ts contains "export async function createPurchaseOrder"
    - src/tools/procurement/po-tools.ts contains "interruptForApproval"
    - src/tools/procurement/po-tools.ts contains "Purchase order creation cancelled by user."
    - src/tools/procurement/index.ts contains "export { searchSuppliers"
    - media-tools.test.ts passes all tests
    - supplier-tools.test.ts passes all tests
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>PR: 4 tools (press release + media monitoring + coverage tracking + sentiment). Procurement: 4 tools (supplier search + quote comparison + PO with HITL + vendor scoring). Both barrel indexes export all. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Data Analyst tools + Operations tools + tests + barrels</name>
  <files>
    worrylesssuperagent/langgraph-server/src/tools/data-analyst/query-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/data-analyst/analysis-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/data-analyst/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/data-analyst/query-tools.test.ts,
    worrylesssuperagent/langgraph-server/src/tools/operations/project-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/operations/process-tools.ts,
    worrylesssuperagent/langgraph-server/src/tools/operations/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/operations/project-tools.test.ts
  </files>
  <read_first>
    worrylesssuperagent/langgraph-server/src/tools/data-analyst/types.ts,
    worrylesssuperagent/langgraph-server/src/tools/operations/types.ts,
    worrylesssuperagent/langgraph-server/src/tools/shared/db.ts,
    worrylesssuperagent/langgraph-server/src/tools/accountant/anomaly-tools.ts,
    worrylesssuperagent/langgraph-server/src/llm/client.ts,
    .planning/phases/15-personal-assistant-operational-agents/15-RESEARCH.md,
    .planning/phases/15-personal-assistant-operational-agents/15-UI-SPEC.md
  </read_first>
  <action>
**DATA ANALYST TOOLS (OPS-06):**

**Create `src/tools/data-analyst/query-tools.ts`** with 2 functions:

1. **`crossFunctionalQuery(userId: string, queryType: string): Promise<QueryResult>`**
   - Use the exact QUERY_TEMPLATES pattern from 15-RESEARCH.md "Data Analyst Cross-Functional Query" section
   - Pre-defined safe SQL templates ONLY (never LLM-generated SQL):
     - `revenue_by_month`: SELECT date_trunc('month', date) AS month, SUM(amount) AS total FROM public.transactions WHERE user_id = $1 AND type = 'income' GROUP BY 1 ORDER BY 1
     - `expenses_by_category`: SELECT category, SUM(amount) AS total FROM public.transactions WHERE user_id = $1 AND type = 'expense' GROUP BY 1 ORDER BY 2 DESC
     - `leads_by_status`: SELECT status, COUNT(*) AS count, COALESCE(SUM(deal_value), 0) AS total_value FROM public.leads WHERE user_id = $1 GROUP BY 1
     - `invoice_summary`: SELECT status, COUNT(*) AS count, SUM(amount) AS total FROM public.invoices WHERE user_id = $1 GROUP BY 1
     - `posts_by_platform`: SELECT platform, COUNT(*) AS count, AVG(engagement_likes) AS avg_likes FROM public.social_posts WHERE user_id = $1 GROUP BY 1
     - `support_ticket_summary`: SELECT status, priority, COUNT(*) AS count FROM public.support_tickets WHERE user_id = $1 GROUP BY 1, 2
     - `project_status`: SELECT status, COUNT(*) AS count FROM public.projects WHERE user_id = $1 GROUP BY 1
   - If unknown queryType, message from UI-SPEC: `"Unknown query type: \"{queryType}\". Available: {availableTypes}"`
   - Empty result message: `"Query returned no results for \"{queryType}\"."`
   - Result message: `"Query complete: {rowCount} row(s) returned for \"{queryType}\"."`

2. **`kpiAggregation(userId: string): Promise<KPISummary>`**
   - Run multiple QUERY_TEMPLATES queries (revenue, leads, invoices, tickets, projects)
   - Calculate derived KPIs: total revenue, conversion rate (closed_won/total leads), avg invoice value, open tickets, active projects
   - Message from UI-SPEC: `"KPI summary: {kpiCount} metric(s) aggregated across {tableCount} data source(s)."`

**Create `src/tools/data-analyst/analysis-tools.ts`** with 3 functions:

3. **`statisticalAnalysis(userId: string, queryType: string): Promise<{ mean: number; median: number; stdDev: number; min: number; max: number; count: number }>`**
   - Run the corresponding QUERY_TEMPLATE, calculate statistics on the numeric column
   - Pure math on DB results, no LLM needed

4. **`anomalyDetection(userId: string, queryType: string): Promise<{ anomalies: AnomalyResult[]; message: string }>`**
   - Reuse z-score pattern from accountant's anomaly-tools.ts
   - Calculate mean and stdDev from query results, flag rows where |z-score| > 2
   - Anomaly message from UI-SPEC: `"Anomaly detected: {description} (z-score: {zScore})"`
   - No anomalies message: `"No anomalies detected in {datasetName}."`

5. **`generateChart(userId: string, queryType: string, chartType: "bar" | "line" | "pie" | "area"): Promise<ChartData>`**
   - Run the corresponding query, format result as Recharts-compatible ChartData
   - Auto-detect xKey and yKey from result columns
   - Message from UI-SPEC: `"Chart data prepared: {chartType} with {dataPointCount} data points."`

**Create `src/tools/data-analyst/index.ts`** barrel:
```typescript
export { crossFunctionalQuery, kpiAggregation } from "./query-tools.js";
export { statisticalAnalysis, anomalyDetection, generateChart } from "./analysis-tools.js";
export type { DAClassification, QueryResult, ChartData, AnomalyResult, KPISummary } from "./types.js";
```

**Create `src/tools/data-analyst/query-tools.test.ts`**: Mock db.ts getPool. Tests: crossFunctionalQuery returns results for valid type, crossFunctionalQuery returns error for unknown type, kpiAggregation aggregates multiple sources, anomalyDetection flags outliers.

---

**OPERATIONS TOOLS (OPS-07):**

**Create `src/tools/operations/project-tools.ts`** with 5 functions:

1. **`createProject(userId: string, name: string, description?: string, startDate?: string, dueDate?: string): Promise<{ projectId: string; message: string }>`**
   - INSERT into public.projects RETURNING id
   - Message from UI-SPEC: `"Project created: \"{name}\""`

2. **`addMilestone(userId: string, projectId: string, title: string, dueDate?: string, owner?: string): Promise<{ milestoneId: string; message: string }>`**
   - Verify project belongs to user first: SELECT id FROM projects WHERE id=$1 AND user_id=$2
   - INSERT into public.project_milestones RETURNING id
   - Message from UI-SPEC: `"Milestone added to \"{projectName}\": \"{milestoneTitle}\""`

3. **`listProjects(userId: string, status?: string): Promise<{ projects: ProjectRow[]; summary: ProjectStatusSummary; message: string }>`**
   - SELECT * FROM public.projects WHERE user_id=$1 [AND status=$2] ORDER BY created_at DESC
   - Calculate ProjectStatusSummary from results
   - Empty message from UI-SPEC: `"No projects found."`
   - Summary message: `"{activeCount} active, {planningCount} planning, {onHoldCount} on hold, {completedCount} completed"`

4. **`trackMilestones(userId: string, projectId: string): Promise<{ milestones: MilestoneRow[]; message: string }>`**
   - SELECT pm.* FROM project_milestones pm JOIN projects p ON pm.project_id = p.id WHERE p.user_id=$1 AND pm.project_id=$2 ORDER BY pm.due_date
   - Allow updating status: if a milestoneId and newStatus are provided, UPDATE and return updated list

5. **`analyzeBottlenecks(userId: string): Promise<BottleneckAnalysis>`**
   - SELECT pm.title, pm.due_date, pm.owner, p.name as project_name FROM project_milestones pm JOIN projects p ON pm.project_id = p.id WHERE p.user_id=$1 AND pm.status='blocked'
   - Message from UI-SPEC: `"{blockedCount} blocked milestone(s) found across {projectCount} project(s)."` or `"No bottlenecks detected. All milestones are on track."`

**Create `src/tools/operations/process-tools.ts`** with 1 function:

6. **`draftSOP(userId: string, processName: string, context?: string): Promise<SOPDocument>`**
   - Call `callLLMWithStructuredOutput` with schema: `'{ "title": "string", "purpose": "string", "steps": [{ "stepNumber": number, "action": "string", "responsible": "string", "notes": "string" }] }'`
   - System prompt: "Draft a Standard Operating Procedure. Be specific and actionable. Each step should be clear enough for someone unfamiliar with the process."
   - Message from UI-SPEC: `"SOP drafted: \"{title}\""`

**Create `src/tools/operations/index.ts`** barrel:
```typescript
export { createProject, addMilestone, listProjects, trackMilestones, analyzeBottlenecks } from "./project-tools.js";
export { draftSOP } from "./process-tools.js";
export type { OpsClassification, ProjectRow, MilestoneRow, BottleneckAnalysis, SOPDocument, ProjectStatusSummary } from "./types.js";
```

**Create `src/tools/operations/project-tools.test.ts`**: Mock db.ts getPool. Tests: createProject inserts, addMilestone inserts with project ownership check, listProjects returns rows with summary, analyzeBottlenecks finds blocked milestones, draftSOP calls LLM.
  </action>
  <verify>
    <automated>cd worrylesssuperagent/langgraph-server && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/tools/data-analyst/query-tools.test.ts src/tools/operations/project-tools.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/tools/data-analyst/query-tools.ts contains "QUERY_TEMPLATES"
    - src/tools/data-analyst/query-tools.ts contains "export async function crossFunctionalQuery"
    - src/tools/data-analyst/query-tools.ts contains "export async function kpiAggregation"
    - src/tools/data-analyst/query-tools.ts does NOT contain any dynamic SQL generation from LLM
    - src/tools/data-analyst/analysis-tools.ts contains "export async function anomalyDetection"
    - src/tools/data-analyst/analysis-tools.ts contains "export async function generateChart"
    - src/tools/data-analyst/analysis-tools.ts contains "zScore" or "z-score" or "z_score"
    - src/tools/data-analyst/index.ts contains "export { crossFunctionalQuery"
    - src/tools/operations/project-tools.ts contains "export async function createProject"
    - src/tools/operations/project-tools.ts contains "export async function addMilestone"
    - src/tools/operations/project-tools.ts contains "export async function analyzeBottlenecks"
    - src/tools/operations/project-tools.ts contains "public.projects"
    - src/tools/operations/project-tools.ts contains "project_milestones"
    - src/tools/operations/process-tools.ts contains "export async function draftSOP"
    - src/tools/operations/index.ts contains "export { createProject"
    - query-tools.test.ts passes all tests
    - project-tools.test.ts passes all tests
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Data Analyst: 5 tools (cross-functional query + KPI + stats + anomaly + chart). Operations: 6 tools (project CRUD + milestones + bottleneck analysis + SOP drafting). Both barrel indexes export all. Tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vitest run src/tools/pr/ src/tools/procurement/ src/tools/data-analyst/ src/tools/operations/ --reporter=verbose` passes all tests
- All 4 barrel indexes export all tools
- Data Analyst uses QUERY_TEMPLATES only (no LLM-generated SQL)
- Procurement PO uses HITL via interruptForApproval
- UI-SPEC copy strings used in all tool output messages
</verification>

<success_criteria>
- OPS-04: PR drafts press releases, monitors media via Firecrawl, tracks coverage, analyzes sentiment
- OPS-05: Procurement searches suppliers, compares quotes, creates POs with HITL, scores vendors
- OPS-06: Data Analyst runs safe parameterized queries across all business tables, detects anomalies, generates chart data
- OPS-07: Operations creates projects with milestones, analyzes bottlenecks, drafts SOPs
</success_criteria>

<output>
After completion, create `.planning/phases/15-personal-assistant-operational-agents/15-04-SUMMARY.md`
</output>
