---
phase: 22-api-server
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - api-server/src/routes/crawlWebsite.ts
  - api-server/src/routes/parseDatasheet.ts
  - api-server/src/routes/generateLeads.ts
  - api-server/src/routes/generateOutreach.ts
  - api-server/src/routes/planningAgent.ts
  - api-server/src/routes/syncGmailCalendar.ts
  - api-server/src/index.ts
autonomous: true
requirements:
  - API-08
  - API-09
  - API-10
  - API-11
  - API-12
  - API-13

must_haves:
  truths:
    - "POST /api/crawl-business-website crawls a URL via Firecrawl API and stores artifacts in the database"
    - "POST /api/parse-datasheet parses CSV/TSV data and returns structured output"
    - "POST /api/generate-leads calls Apify API to generate lead lists"
    - "POST /api/generate-outreach generates outreach messages using Gemini"
    - "POST /api/planning-agent generates task plans using Gemini and stores them in the database"
    - "POST /api/sync-gmail-calendar syncs Gmail/Calendar data using stored Google OAuth tokens"
    - "All 6 routes reject requests without valid JWT with 401"
    - "User ID comes from JWT sub claim in all routes"
  artifacts:
    - path: "api-server/src/routes/crawlWebsite.ts"
      provides: "Website crawl route using Firecrawl API"
      exports: ["crawlWebsite"]
    - path: "api-server/src/routes/parseDatasheet.ts"
      provides: "CSV/TSV parsing route"
      exports: ["parseDatasheet"]
    - path: "api-server/src/routes/generateLeads.ts"
      provides: "Lead generation route using Apify"
      exports: ["generateLeads"]
    - path: "api-server/src/routes/generateOutreach.ts"
      provides: "Outreach message generation route"
      exports: ["generateOutreach"]
    - path: "api-server/src/routes/planningAgent.ts"
      provides: "Task planning route using Gemini"
      exports: ["planningAgent"]
    - path: "api-server/src/routes/syncGmailCalendar.ts"
      provides: "Gmail/Calendar sync route"
      exports: ["syncGmailCalendar"]
  key_links:
    - from: "api-server/src/routes/crawlWebsite.ts"
      to: "Firecrawl API"
      via: "fetch with FIRECRAWL_API_KEY"
      pattern: "process\\.env\\.FIRECRAWL_API_KEY"
    - from: "api-server/src/routes/generateLeads.ts"
      to: "Apify API"
      via: "fetch with APIFY_API_TOKEN"
      pattern: "process\\.env\\.APIFY_API_TOKEN"
    - from: "api-server/src/routes/syncGmailCalendar.ts"
      to: "Google APIs"
      via: "OAuth token from integrations table"
      pattern: "pool\\.query.*integrations"
---

<objective>
Implement the 6 business data routes: crawl-business-website, parse-datasheet, generate-leads, generate-outreach, planning-agent, and sync-gmail-calendar. These are the business intelligence tools used by the Sales Rep, Accountant, and Personal Assistant agents.

Purpose: These routes power the agent tools for lead generation, content research, data parsing, outreach campaigns, task planning, and calendar integration. They connect to external APIs (Firecrawl, Apify, Google) and use Gemini for LLM tasks.
Output: 6 route files registered in index.ts, all compiling cleanly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-api-server/22-RESEARCH.md
@.planning/phases/22-api-server/22-01-SUMMARY.md

<interfaces>
<!-- From api-server (created in 22-01): -->
```typescript
// middleware/auth.ts
export interface AuthedRequest extends Request { auth?: { userId: string; payload: JWTPayload } }
export async function verifyLogtoJWT(req: AuthedRequest, res: Response, next: NextFunction): Promise<void>;

// db/pool.ts
export const pool: Pool;

// lib/gemini.ts
export const geminiOpenAI: OpenAI;
// Usage: geminiOpenAI.chat.completions.create({ model: 'gemini-2.0-flash', messages: [...] })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port crawl-business-website, parse-datasheet, and generate-leads routes</name>
  <files>
    api-server/src/routes/crawlWebsite.ts
    api-server/src/routes/parseDatasheet.ts
    api-server/src/routes/generateLeads.ts
    api-server/src/index.ts
  </files>
  <read_first>
    worrylesssuperagent/supabase/functions/crawl-business-website/index.ts
    worrylesssuperagent/supabase/functions/parse-datasheet/index.ts
    worrylesssuperagent/supabase/functions/generate-leads/index.ts
    api-server/src/index.ts
    api-server/src/db/pool.ts
    api-server/src/lib/gemini.ts
  </read_first>
  <action>
Port 3 edge functions following the standard translation pattern:
- `Deno.env.get('KEY')` -> `process.env.KEY`
- `serve(async (req) => {...})` -> `export const routeName: RequestHandler = async (req, res) => {...}`
- Remove CORS headers, OPTIONS handling (handled by cors middleware)
- Remove `createClient(url, key)` auth check -> `(req as AuthedRequest).auth!.userId`
- `supabase.from('table').select/insert/update(...)` -> `pool.query('SQL', [params])` with `.rows`
- `fetch(LOVABLE_AI_GATEWAY, ...)` -> `geminiOpenAI.chat.completions.create({ model: 'gemini-2.0-flash', ... })`
- `return new Response(JSON.stringify(data))` -> `res.json(data)`

**crawl-business-website (376 lines):**
Create `api-server/src/routes/crawlWebsite.ts`:
- Uses `process.env.FIRECRAWL_API_KEY` for the Firecrawl API call (external `fetch` call — keep as-is, just change env var access)
- Replace `supabaseAdmin.from('business_artifacts').insert(...)` with `pool.query('INSERT INTO business_artifacts (...) VALUES ($1, $2, ...) RETURNING *', [...])`
- Replace any LLM calls with `geminiOpenAI.chat.completions.create()`
- User ID from JWT: `(req as AuthedRequest).auth!.userId`

**parse-datasheet (254 lines):**
Create `api-server/src/routes/parseDatasheet.ts`:
- This function contains inline CSV/TSV parsing logic — it's pure TS with no Deno-specific APIs beyond `Deno.env.get` and `serve()`
- Copy the parsing logic directly
- Replace any LLM calls for data extraction with `geminiOpenAI.chat.completions.create()`
- Replace any DB calls with `pool.query()`

**generate-leads (385 lines):**
Create `api-server/src/routes/generateLeads.ts`:
- Uses `process.env.APIFY_API_TOKEN` for Apify API calls (external fetch — keep as-is)
- Replace `supabaseAdmin.from('leads')` calls with `pool.query()` parameterized queries
- Replace any LLM calls with `geminiOpenAI.chat.completions.create()`

Register all 3 in `api-server/src/index.ts`:
```typescript
import { crawlWebsite } from './routes/crawlWebsite.js';
import { parseDatasheet } from './routes/parseDatasheet.js';
import { generateLeads } from './routes/generateLeads.js';

app.post('/api/crawl-business-website', verifyLogtoJWT, crawlWebsite);
app.post('/api/parse-datasheet', verifyLogtoJWT, parseDatasheet);
app.post('/api/generate-leads', verifyLogtoJWT, generateLeads);
```
  </action>
  <verify>
    <automated>cd api-server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - api-server/src/routes/crawlWebsite.ts contains `export const crawlWebsite`
    - api-server/src/routes/crawlWebsite.ts contains `FIRECRAWL_API_KEY`
    - api-server/src/routes/crawlWebsite.ts contains `pool.query`
    - api-server/src/routes/crawlWebsite.ts does NOT contain `Deno.env.get`
    - api-server/src/routes/parseDatasheet.ts contains `export const parseDatasheet`
    - api-server/src/routes/parseDatasheet.ts does NOT contain `Deno.env.get`
    - api-server/src/routes/generateLeads.ts contains `export const generateLeads`
    - api-server/src/routes/generateLeads.ts contains `APIFY_API_TOKEN`
    - api-server/src/routes/generateLeads.ts does NOT contain `Deno.env.get`
    - api-server/src/index.ts contains `app.post('/api/crawl-business-website'`
    - api-server/src/index.ts contains `app.post('/api/parse-datasheet'`
    - api-server/src/index.ts contains `app.post('/api/generate-leads'`
    - `cd api-server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>crawl-business-website, parse-datasheet, and generate-leads routes ported with Firecrawl/Apify API passthrough, pg pool for DB, and Gemini for LLM calls.</done>
</task>

<task type="auto">
  <name>Task 2: Port generate-outreach, planning-agent, and sync-gmail-calendar routes</name>
  <files>
    api-server/src/routes/generateOutreach.ts
    api-server/src/routes/planningAgent.ts
    api-server/src/routes/syncGmailCalendar.ts
    api-server/src/index.ts
  </files>
  <read_first>
    worrylesssuperagent/supabase/functions/generate-outreach/index.ts
    worrylesssuperagent/supabase/functions/planning-agent/index.ts
    worrylesssuperagent/supabase/functions/sync-gmail-calendar/index.ts
    api-server/src/index.ts
    api-server/src/db/pool.ts
    api-server/src/lib/gemini.ts
  </read_first>
  <action>
Port 3 edge functions using the same translation pattern as Task 1.

**generate-outreach (212 lines):**
Create `api-server/src/routes/generateOutreach.ts`:
- Replace Lovable gateway with `geminiOpenAI.chat.completions.create({ model: 'gemini-2.0-flash', ... })`
- Replace any DB calls with `pool.query()`
- User ID from JWT
- This route generates email/LinkedIn outreach messages — the LLM prompt stays the same, only the transport layer changes

**planning-agent (479 lines):**
Create `api-server/src/routes/planningAgent.ts`:
- This is a larger function — read the edge function carefully
- Replace LLM calls with `geminiOpenAI.chat.completions.create()`
- Replace `supabase.from('tasks').insert(...)` / `update(...)` with `pool.query()` parameterized SQL
- May contain multi-step LLM reasoning — preserve the prompt chains unchanged
- User ID from JWT

**sync-gmail-calendar (102 lines):**
Create `api-server/src/routes/syncGmailCalendar.ts`:
- This route reads Google OAuth tokens from the `integrations` table: `pool.query('SELECT access_token, refresh_token FROM integrations WHERE user_id = $1 AND provider = $2', [userId, 'google'])`
- Uses Google APIs (googleapis npm package) — the package needs to be in dependencies. Check if it's already listed; if not, add `"googleapis": "^171.4.0"` to api-server/package.json and run `npm install`
- Replace `supabase.from('integrations').select(...)` with `pool.query()`
- The Google API calls (Gmail list, Calendar events) stay the same — only the auth token retrieval changes

Register all 3 in `api-server/src/index.ts`:
```typescript
import { generateOutreach } from './routes/generateOutreach.js';
import { planningAgent } from './routes/planningAgent.js';
import { syncGmailCalendar } from './routes/syncGmailCalendar.js';

app.post('/api/generate-outreach', verifyLogtoJWT, generateOutreach);
app.post('/api/planning-agent', verifyLogtoJWT, planningAgent);
app.post('/api/sync-gmail-calendar', verifyLogtoJWT, syncGmailCalendar);
```
  </action>
  <verify>
    <automated>cd api-server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - api-server/src/routes/generateOutreach.ts contains `export const generateOutreach`
    - api-server/src/routes/generateOutreach.ts contains `geminiOpenAI.chat.completions.create`
    - api-server/src/routes/generateOutreach.ts does NOT contain `Deno.env.get`
    - api-server/src/routes/planningAgent.ts contains `export const planningAgent`
    - api-server/src/routes/planningAgent.ts contains `pool.query`
    - api-server/src/routes/planningAgent.ts does NOT contain `Deno.env.get`
    - api-server/src/routes/syncGmailCalendar.ts contains `export const syncGmailCalendar`
    - api-server/src/routes/syncGmailCalendar.ts does NOT contain `Deno.env.get`
    - api-server/src/routes/syncGmailCalendar.ts does NOT contain `createClient`
    - api-server/src/index.ts contains `app.post('/api/generate-outreach'`
    - api-server/src/index.ts contains `app.post('/api/planning-agent'`
    - api-server/src/index.ts contains `app.post('/api/sync-gmail-calendar'`
    - `cd api-server && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>generate-outreach, planning-agent, and sync-gmail-calendar routes ported with Gemini LLM, pg pool for DB, and Google APIs for calendar/gmail sync.</done>
</task>

</tasks>

<verification>
- `cd api-server && npx tsc --noEmit` — zero type errors
- `grep -rn "Deno" api-server/src/routes/` — zero hits across all route files
- `grep -rn "createClient" api-server/src/routes/` — zero hits
- `grep -rn "LOVABLE" api-server/src/routes/` — zero hits
- `grep -c "app.post" api-server/src/index.ts` — count shows all routes registered
</verification>

<success_criteria>
- 6 route files exist and compile cleanly
- All use pg pool for DB access (not supabase-js)
- All LLM calls use Gemini OpenAI-compat endpoint
- External API calls (Firecrawl, Apify, Google) use process.env for keys
- All registered in index.ts with verifyLogtoJWT
- No Deno globals remain
</success_criteria>

<output>
After completion, create `.planning/phases/22-api-server/22-04-SUMMARY.md`
</output>
