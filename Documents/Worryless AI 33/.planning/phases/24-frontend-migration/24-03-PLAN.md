---
phase: 24-frontend-migration
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - worrylesssuperagent/src/hooks/useNotifications.ts
  - worrylesssuperagent/src/hooks/useTeamData.ts
  - worrylesssuperagent/src/hooks/useHeartbeatConfig.ts
  - worrylesssuperagent/src/hooks/useCadenceConfig.ts
  - worrylesssuperagent/src/hooks/useAgentWorkspace.ts
  - worrylesssuperagent/src/hooks/useAgentMarketplace.ts
  - worrylesssuperagent/src/hooks/usePushSubscription.ts
  - worrylesssuperagent/src/hooks/useAgentChat.ts
  - worrylesssuperagent/src/hooks/useLangGraphFlag.ts
autonomous: true
requirements: [FE-01, FE-02, FE-04]

must_haves:
  truths:
    - "useNotifications fetches from /api/notifications via api.ts with React Query polling at 30s"
    - "useTeamData fetches from /api/team-data via api.ts with React Query polling at 60s"
    - "useHeartbeatConfig and useCadenceConfig fetch/update via /api/user-agents/:agentTypeId/heartbeat and /cadence"
    - "useAgentWorkspace fetches/updates via /api/workspaces/:agentTypeId/:fileType"
    - "useAgentMarketplace fetches via /api/agent-types and /api/user-agents"
    - "usePushSubscription calls /api/push-subscriptions via api.ts"
    - "useAgentChat sends SSE to VITE_API_URL/api/langgraph-proxy with Logto Bearer token"
    - "useLangGraphFlag.ts is deleted — no dual-mode branching remains"
    - "No hook file imports from @/integrations/supabase/client"
  artifacts:
    - path: "worrylesssuperagent/src/hooks/useNotifications.ts"
      provides: "Notifications with React Query polling"
      contains: "useQuery"
    - path: "worrylesssuperagent/src/hooks/useTeamData.ts"
      provides: "Team data with React Query polling"
      contains: "useQuery"
    - path: "worrylesssuperagent/src/hooks/useAgentChat.ts"
      provides: "SSE chat via VITE_API_URL"
      contains: "VITE_API_URL"
  key_links:
    - from: "worrylesssuperagent/src/hooks/useNotifications.ts"
      to: "worrylesssuperagent/src/lib/api.ts"
      via: "api.get import"
      pattern: "import.*api.*from.*@/lib/api"
    - from: "worrylesssuperagent/src/hooks/useAgentChat.ts"
      to: "api-server langgraph-proxy"
      via: "VITE_API_URL/api/langgraph-proxy"
      pattern: "VITE_API_URL.*langgraph-proxy"
---

<objective>
Migrate all 9 data-fetching hooks from Supabase client calls to api.ts + React Query, and migrate useAgentChat SSE to Railway LangGraph URL.

Purpose: The hooks are the data layer between components and the backend. Once hooks use api.ts instead of supabase.from(), all components automatically get Railway API connectivity. React Query (per D-04) replaces the manual useState+useEffect pattern with caching, polling, and invalidation. useAgentChat's SSE URL changes from Supabase function URL to VITE_API_URL/api/langgraph-proxy.

Output: All 9 hooks migrated, useLangGraphFlag.ts deleted, zero supabase imports in any hook file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-frontend-migration/24-CONTEXT.md
@.planning/phases/24-frontend-migration/24-RESEARCH.md
@.planning/phases/24-frontend-migration/24-01-SUMMARY.md

@worrylesssuperagent/src/lib/api.ts
@worrylesssuperagent/src/hooks/useAuth.ts

<interfaces>
From worrylesssuperagent/src/lib/api.ts (created in Plan 01):
```typescript
export const api = {
  get: <T>(path: string, options?: ApiOptions) => Promise<T>,
  post: <T>(path: string, body: unknown, options?: ApiOptions) => Promise<T>,
  patch: <T>(path: string, body: unknown, options?: ApiOptions) => Promise<T>,
  delete: <T>(path: string, options?: ApiOptions) => Promise<T>,
};
```

From worrylesssuperagent/src/hooks/useAuth.ts:
```typescript
export function useAuth(): {
  userId: string | null;
  token: string | null;
  isAuthenticated: boolean;
  signIn: () => void;
  signOut: () => void;
}
```

React Query v5 pattern:
```typescript
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
const { data, isLoading } = useQuery({
  queryKey: ["key", param],
  queryFn: () => api.get<Type>("/api/endpoint", { token }),
  enabled: !!token,
  refetchInterval: 30_000,
  staleTime: 20_000,
});
```

API routes available (from Plans 01+02):
- GET /api/profiles/me, PATCH /api/profiles/me
- GET /api/notifications, PATCH /api/notifications/:id, POST /api/notifications/mark-all-read
- GET /api/team-data
- GET/POST/PATCH/DELETE /api/tasks
- GET/POST/DELETE /api/artifacts
- GET/POST/PATCH /api/user-agents, GET/PATCH /api/user-agents/:agentTypeId/heartbeat, GET/PATCH /api/user-agents/:agentTypeId/cadence
- GET /api/agent-types
- GET/PATCH /api/workspaces/:agentTypeId/:fileType
- GET/POST/DELETE /api/leads, /api/social-posts, /api/invoices, /api/datasheets
- GET/POST /api/transactions, /api/outreach-emails
- POST/DELETE /api/push-subscriptions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate read-only and config hooks to React Query + api.ts</name>
  <files>
    worrylesssuperagent/src/hooks/useNotifications.ts,
    worrylesssuperagent/src/hooks/useTeamData.ts,
    worrylesssuperagent/src/hooks/useHeartbeatConfig.ts,
    worrylesssuperagent/src/hooks/useCadenceConfig.ts,
    worrylesssuperagent/src/hooks/useAgentWorkspace.ts,
    worrylesssuperagent/src/hooks/useAgentMarketplace.ts,
    worrylesssuperagent/src/hooks/usePushSubscription.ts
  </files>
  <read_first>
    - worrylesssuperagent/src/hooks/useNotifications.ts
    - worrylesssuperagent/src/hooks/useTeamData.ts
    - worrylesssuperagent/src/hooks/useHeartbeatConfig.ts
    - worrylesssuperagent/src/hooks/useCadenceConfig.ts
    - worrylesssuperagent/src/hooks/useAgentWorkspace.ts
    - worrylesssuperagent/src/hooks/useAgentMarketplace.ts
    - worrylesssuperagent/src/hooks/usePushSubscription.ts
    - worrylesssuperagent/src/lib/api.ts
    - worrylesssuperagent/src/hooks/useAuth.ts
  </read_first>
  <action>
Rewrite each hook to use `api.ts` + React Query (per D-04, D-08). The return type signatures MUST stay identical so consuming components don't break.

**General pattern for all hooks:**
1. Remove `import { supabase } from '@/integrations/supabase/client'`
2. Add `import { api } from '@/lib/api'` and `import { useAuth } from '@/hooks/useAuth'`
3. Add `import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'`
4. Replace `useState` + `useEffect` data loading with `useQuery({ queryKey, queryFn, enabled: !!token })`
5. Replace mutation calls (insert/update/delete) with `useMutation({ mutationFn, onSuccess: () => queryClient.invalidateQueries({ queryKey }) })`
6. Remove ALL Supabase Realtime channel setup/teardown — React Query `refetchInterval` replaces it
7. Preserve the exact return type signature so components don't break

**useNotifications.ts** (per D-08, D-10):
- `useQuery` with `queryKey: ['notifications', userId]`, `queryFn: () => api.get<Notification[]>('/api/notifications', { token })`, `refetchInterval: 30_000`, `staleTime: 20_000`, `enabled: !!userId && !!token`
- `useMutation` for markRead: `api.patch('/api/notifications/${id}', { is_read: true }, { token })`, onSuccess invalidates `['notifications', userId]`
- `useMutation` for markAllRead: `api.post('/api/notifications/mark-all-read', {}, { token })`, onSuccess invalidates `['notifications', userId]`
- Compute `unreadCount` from `notifications.filter(n => !n.is_read).length`
- Remove the `supabase.channel('notifications')` Realtime subscription entirely
- Keep the `Notification` interface export and `LEGACY_VIEW_MAP` and `resolveView` function unchanged

**useTeamData.ts** (per D-08):
- `useQuery` with `queryKey: ['team-data', userId]`, `queryFn: () => api.get<TeamAgent[]>('/api/team-data', { token })`, `refetchInterval: 60_000`, `staleTime: 45_000`, `enabled: !!userId && !!token`
- Derive `chiefOfStaff` from data by filtering `agentTypeId === 'chief_of_staff'`, rest go to `otherAgents`
- Keep `TeamAgent` interface export unchanged
- Keep `getHeartbeatStatus` import and usage unchanged

**useHeartbeatConfig.ts:**
- `useQuery` with `queryKey: ['heartbeat', agentTypeId]` to `api.get('/api/user-agents/${agentTypeId}/heartbeat', { token })`
- `useMutation` for update to `api.patch('/api/user-agents/${agentTypeId}/heartbeat', patch, { token })`, onSuccess invalidates `['heartbeat', agentTypeId]`
- Keep return type signature identical

**useCadenceConfig.ts:**
- Same pattern as useHeartbeatConfig but with `queryKey: ['cadence', agentTypeId]` and `/api/user-agents/${agentTypeId}/cadence` endpoint

**useAgentWorkspace.ts:**
- `useQuery` with `queryKey: ['workspace', agentTypeId, fileType]` to `api.get('/api/workspaces/${agentTypeId}/${fileType}', { token })`
- `useMutation` for save to `api.patch('/api/workspaces/${agentTypeId}/${fileType}', { content }, { token })`
- Keep workspace type and return signature unchanged

**useAgentMarketplace.ts:**
- Two queries: `useQuery` for agent types `api.get('/api/agent-types', { token })` and `useQuery` for user agents `api.get('/api/user-agents', { token })`
- `useMutation` for activate/deactivate: `api.post` or `api.patch` on `/api/user-agents`
- Keep return type (installed agents, available agents, toggle function) unchanged

**usePushSubscription.ts:**
- Replace `supabase.from('push_subscriptions')` INSERT/DELETE with `api.post('/api/push-subscriptions', { endpoint, keys }, { token })` and `api.delete('/api/push-subscriptions', { token })` (note: delete uses body with endpoint)
- Actually, the delete endpoint in the API server uses `req.body.endpoint` so send as POST-style delete OR adjust: the API server's `deletePushSubscription` handler reads endpoint from `req.body`, so the frontend should send `fetch(url, { method: 'DELETE', body: JSON.stringify({ endpoint }) })`. The api.ts `delete` method doesn't send a body — add a `deleteWithBody` method OR use `api.post('/api/push-subscriptions/unsubscribe', { endpoint }, { token })`. SIMPLEST: modify api.ts to accept optional body on delete, OR just use raw `fetch` for this one call. Use `fetch` directly for the delete call since api.ts delete doesn't support body.
- Keep return type (subscribe, unsubscribe, isSubscribed) unchanged
  </action>
  <verify>
    <automated>cd "/Users/anthonysure/Documents/Worryless AI 33/worrylesssuperagent" && grep -c "supabase" src/hooks/useNotifications.ts src/hooks/useTeamData.ts src/hooks/useHeartbeatConfig.ts src/hooks/useCadenceConfig.ts src/hooks/useAgentWorkspace.ts src/hooks/useAgentMarketplace.ts src/hooks/usePushSubscription.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - useNotifications.ts contains `import { api }` and `import { useQuery` and `refetchInterval: 30_000` and does NOT contain `supabase`
    - useTeamData.ts contains `import { api }` and `import { useQuery` and `refetchInterval: 60_000` and does NOT contain `supabase`
    - useHeartbeatConfig.ts contains `import { api }` and `import { useQuery` and does NOT contain `supabase`
    - useCadenceConfig.ts contains `import { api }` and `import { useQuery` and does NOT contain `supabase`
    - useAgentWorkspace.ts contains `import { api }` and `import { useQuery` and does NOT contain `supabase`
    - useAgentMarketplace.ts contains `import { api }` and `import { useQuery` and does NOT contain `supabase`
    - usePushSubscription.ts does NOT contain `supabase`
    - None of the 7 files contain `supabase.channel` or `removeChannel` (Realtime removed)
    - All 7 files contain `import { useAuth }` (token access)
  </acceptance_criteria>
  <done>All 7 hooks migrated from supabase.from() to api.ts + React Query, Realtime channels removed, return signatures preserved</done>
</task>

<task type="auto">
  <name>Task 2: Migrate useAgentChat to Railway SSE + delete useLangGraphFlag</name>
  <files>
    worrylesssuperagent/src/hooks/useAgentChat.ts,
    worrylesssuperagent/src/hooks/useLangGraphFlag.ts
  </files>
  <read_first>
    - worrylesssuperagent/src/hooks/useAgentChat.ts
    - worrylesssuperagent/src/hooks/useLangGraphFlag.ts
    - worrylesssuperagent/src/hooks/useAuth.ts
    - worrylesssuperagent/src/lib/api.ts
  </read_first>
  <action>
**useAgentChat.ts migration per D-18, D-19:**

1. Remove `import { supabase } from "@/integrations/supabase/client"`
2. Remove `import { useLangGraphFlag }` and `import { getChatEndpoint }` (or any imports from useLangGraphFlag.ts)
3. Add `import { useAuth } from "@/hooks/useAuth"` at top
4. Add `import { api } from "@/lib/api"` for any non-SSE data fetches (e.g., profiles query)
5. Add at the hook's top level: `const { token, userId } = useAuth();`
6. Add: `const API_URL = import.meta.env.VITE_API_URL as string;`
7. Remove `const supabaseUrl = import.meta.env.VITE_SUPABASE_URL` (or similar)
8. Remove ALL `supabase.auth.getSession()` calls — the token is already available from `useAuth()`
9. Replace ALL URL constructions:
   - `${supabaseUrl}/functions/v1/langgraph-proxy/threads/${userId}` becomes `${API_URL}/api/langgraph-proxy/threads/${userId}`
   - `${supabaseUrl}/functions/v1/langgraph-proxy/invoke/stream` becomes `${API_URL}/api/langgraph-proxy/invoke/stream`
   - `${supabaseUrl}/functions/v1/langgraph-proxy/invoke/resume` becomes `${API_URL}/api/langgraph-proxy/invoke/resume`
   - `${supabaseUrl}/functions/v1/langgraph-proxy/invoke` becomes `${API_URL}/api/langgraph-proxy/invoke`
   - `${supabaseUrl}/functions/v1/langgraph-proxy/store` becomes `${API_URL}/api/langgraph-proxy/store`
10. Replace ALL auth headers from `session?.access_token` to `token` from useAuth:
    - `"Authorization": \`Bearer ${session?.access_token}\`` becomes `"Authorization": \`Bearer ${token}\``
    - Or `headers.set("Authorization", ...)` — same replacement
11. Remove the dual-mode `useLangGraph` flag branching entirely — always use the LangGraph path (per D-18: "always use LangGraph path")
12. If there's a `supabase.from("profiles").select("business_stage")` call, replace with `api.get("/api/profiles/me", { token })` and extract `business_stage` from the result
13. Keep the SSE ReadableStream reading logic unchanged — only the URL and auth header change

**Delete useLangGraphFlag.ts:**
- Delete the file `worrylesssuperagent/src/hooks/useLangGraphFlag.ts` entirely
- Search for any other file that imports from useLangGraphFlag — if found, remove that import and the flag usage (should only be useAgentChat.ts)
  </action>
  <verify>
    <automated>cd "/Users/anthonysure/Documents/Worryless AI 33/worrylesssuperagent" && grep -c "supabase" src/hooks/useAgentChat.ts 2>&1 && test ! -f src/hooks/useLangGraphFlag.ts && echo "useLangGraphFlag.ts deleted" || echo "useLangGraphFlag.ts still exists"</automated>
  </verify>
  <acceptance_criteria>
    - useAgentChat.ts does NOT contain `supabase` (zero occurrences)
    - useAgentChat.ts does NOT contain `useLangGraphFlag` or `getChatEndpoint`
    - useAgentChat.ts does NOT contain `VITE_SUPABASE_URL`
    - useAgentChat.ts contains `import { useAuth }` from `@/hooks/useAuth`
    - useAgentChat.ts contains `VITE_API_URL` (Railway API base URL)
    - useAgentChat.ts contains `/api/langgraph-proxy/invoke/stream` (Railway SSE path)
    - useAgentChat.ts contains `Bearer ${token}` (Logto token, not session?.access_token)
    - useLangGraphFlag.ts does NOT exist (file deleted)
    - No other file in src/hooks/ imports from `useLangGraphFlag`
  </acceptance_criteria>
  <done>useAgentChat sends SSE to VITE_API_URL/api/langgraph-proxy with Logto token, useLangGraphFlag.ts deleted, zero supabase references in any hook</done>
</task>

</tasks>

<verification>
- `grep -rc "supabase" worrylesssuperagent/src/hooks/` returns 0 for every hook file
- `test ! -f worrylesssuperagent/src/hooks/useLangGraphFlag.ts` passes
- `grep -c "useQuery" worrylesssuperagent/src/hooks/useNotifications.ts` returns at least 1
- `grep -c "refetchInterval" worrylesssuperagent/src/hooks/useNotifications.ts` returns 1
- `grep "VITE_API_URL" worrylesssuperagent/src/hooks/useAgentChat.ts` matches
</verification>

<success_criteria>
- All 9 hooks migrated: zero supabase imports across all hook files
- useNotifications and useTeamData use React Query with polling (30s and 60s respectively)
- useAgentChat sends SSE to VITE_API_URL/api/langgraph-proxy with Logto Bearer token
- useLangGraphFlag.ts is deleted
- All hook return type signatures preserved (components don't break)
</success_criteria>

<output>
After completion, create `.planning/phases/24-frontend-migration/24-03-SUMMARY.md`
</output>
