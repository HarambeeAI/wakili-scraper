---
phase: 20-database-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worrylesssuperagent/RAILWAY_MIGRATION.sql
autonomous: true
requirements:
  - DB-01
  - DB-03
  - DB-04
  - DB-05

must_haves:
  truths:
    - "RAILWAY_MIGRATION.sql contains zero references to auth.users, auth.uid, pgmq, cron.schedule, vault.decrypted_secrets, or supabase_realtime"
    - "RAILWAY_MIGRATION.sql creates public.users table before any FK references"
    - "RAILWAY_MIGRATION.sql creates langgraph schema with checkpoints, checkpoint_writes, checkpoint_migrations, and store tables"
    - "RAILWAY_MIGRATION.sql creates pgvector extension without schema qualifier"
    - "All 16 auth.users FK references are replaced with public.users references"
  artifacts:
    - path: "worrylesssuperagent/RAILWAY_MIGRATION.sql"
      provides: "Complete sanitized schema for Railway Postgres"
      contains: "CREATE TABLE IF NOT EXISTS public.users"
  key_links:
    - from: "worrylesssuperagent/RAILWAY_MIGRATION.sql"
      to: "Railway Postgres (Phase 19)"
      via: "psql apply in Plan 20-02"
      pattern: "CREATE TABLE.*public\\.users"
---

<objective>
Author RAILWAY_MIGRATION.sql by concatenating all 36 Supabase migration files in chronological order and applying seven sanitization passes to strip all Supabase-specific SQL.

Purpose: Produce a single flat SQL file that applies cleanly to a vanilla Railway PostgreSQL 18 instance with pgvector, creating all 34 application tables, the public.users table, the langgraph schema, and seed data.

Output: `worrylesssuperagent/RAILWAY_MIGRATION.sql`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-database-migration/20-RESEARCH.md
@.planning/phases/19-infrastructure-provisioning/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Concatenate all 36 migration files and apply seven sanitization passes</name>
  <files>worrylesssuperagent/RAILWAY_MIGRATION.sql</files>
  <read_first>
    worrylesssuperagent/supabase/migrations/20251204060048_4cba7ad2-2e1c-4282-919b-6bb6e23ecdaa.sql
    worrylesssuperagent/supabase/migrations/20251204060101_f2d5594d-b70f-4d55-afc5-5eb4c270c780.sql
    worrylesssuperagent/supabase/migrations/20251204062328_1535d79f-0352-4c0d-8927-1c760823f179.sql
    worrylesssuperagent/supabase/migrations/20251204063320_ddc6ce15-8e06-4fb5-8241-bf5c5e9c0992.sql
    worrylesssuperagent/supabase/migrations/20251204071006_400d6fd7-024b-449a-a16f-56168883e6df.sql
    worrylesssuperagent/supabase/migrations/20251204115055_eb6608f5-c48e-4ad4-8439-c1a8481d17bd.sql
    worrylesssuperagent/supabase/migrations/20251204115350_1ca332e1-794f-49bd-83b4-cf9d45f979b0.sql
    worrylesssuperagent/supabase/migrations/20251208095810_ce80ce71-9bcc-47cf-ad1d-9210b145a06a.sql
    worrylesssuperagent/supabase/migrations/20251208123605_68c1946d-5eab-468a-8372-71817f6dfbea.sql
    worrylesssuperagent/supabase/migrations/20251216113439_ff8a2405-037c-41ed-aca6-c4af3bbafaf9.sql
    worrylesssuperagent/supabase/migrations/20251216134813_748bb5e8-dbcc-4a77-97b6-19d76a2b8fe2.sql
    worrylesssuperagent/supabase/migrations/20251216145906_04fefdf8-f6cb-4fcf-9ba9-3324dcd24dc5.sql
    worrylesssuperagent/supabase/migrations/20260112115051_f7c4d2f1-a6c9-4155-b752-3e8bb7cae0a0.sql
    worrylesssuperagent/supabase/migrations/20260113084121_e2836d07-41b7-4401-b75e-b9a2c107f594.sql
    worrylesssuperagent/supabase/migrations/20260312000001_create_agent_tables.sql
    worrylesssuperagent/supabase/migrations/20260312000002_workspace_trigger.sql
    worrylesssuperagent/supabase/migrations/20260312000003_seed_agent_types.sql
    worrylesssuperagent/supabase/migrations/20260312000004_backfill_existing_users.sql
    worrylesssuperagent/supabase/migrations/20260312000005_tools_skill_config_verify.sql
    worrylesssuperagent/supabase/migrations/20260313000006_heartbeat_queue.sql
    worrylesssuperagent/supabase/migrations/20260313000007_heartbeat_cron_jobs.sql
    worrylesssuperagent/supabase/migrations/20260313000008_heartbeat_dispatcher_fn.sql
    worrylesssuperagent/supabase/migrations/20260313000009_morning_digest_cron.sql
    worrylesssuperagent/supabase/migrations/20260313000010_push_subscriptions.sql
    worrylesssuperagent/supabase/migrations/20260313000011_next_digest_run_at.sql
    worrylesssuperagent/supabase/migrations/20260318000001_langgraph_schema.sql
    worrylesssuperagent/supabase/migrations/20260318000002_pgvector_embeddings.sql
    worrylesssuperagent/supabase/migrations/20260318000003_feature_flag.sql
    worrylesssuperagent/supabase/migrations/20260319000001_agent_audit_log.sql
    worrylesssuperagent/supabase/migrations/20260319000002_governance_columns.sql
    worrylesssuperagent/supabase/migrations/20260319000003_acct_sales_schema.sql
    worrylesssuperagent/supabase/migrations/20260319000004_business_stage.sql
    worrylesssuperagent/supabase/migrations/20260320000001_cadence_config.sql
    worrylesssuperagent/supabase/migrations/20260320000001_ops_agent_tables.sql
    worrylesssuperagent/supabase/migrations/20260320000002_cadence_dispatcher_v2.sql
    worrylesssuperagent/supabase/migrations/20260320000003_event_detector.sql
  </read_first>
  <action>
Create `worrylesssuperagent/RAILWAY_MIGRATION.sql` as a single flat SQL file. Process as follows:

**Step 0 — Header:**
Add a comment header at the top:
```sql
-- ============================================================
-- RAILWAY_MIGRATION.sql
-- Sanitized Worryless AI schema for Railway PostgreSQL 18 (pgvector)
-- Generated from 36 Supabase migration files with 7 sanitization passes.
-- Apply with: psql "$DATABASE_URL" -f RAILWAY_MIGRATION.sql
-- WARNING: Do NOT wrap in BEGIN/COMMIT — ALTER TYPE ADD VALUE fails inside transactions.
-- ============================================================
```

**Step 1 — Insert `public.users` table at top (before any migration content):**
```sql
CREATE TABLE IF NOT EXISTS public.users (
  id          UUID        PRIMARY KEY,
  email       TEXT,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**Step 2 — Drop 5 files entirely (Supabase-only content):**
- `20260112115051_f7c4d2f1` (pg_cron + pg_net extension creates + cron grants)
- `20260313000007_heartbeat_cron_jobs` (cron.schedule via vault secrets)
- `20260313000009_morning_digest_cron` (cron.schedule via vault secrets)
- `20260313000011_next_digest_run_at` (cron.schedule + digest via vault)
- `20251216145906_04fefdf8` (storage bucket + storage.objects policies)

These files contribute ZERO application tables; all content is Supabase-specific.

**Step 3 — Concatenate remaining 31 files in chronological order.** Use the file sort order from the migrations directory listing.

**Step 4 — Apply Pass 3 (Replace auth.users FKs):**
Find-and-replace every occurrence of `REFERENCES auth.users(id)` with `REFERENCES public.users(id)`. There are 16 occurrences across 11 files (complete list in RESEARCH.md "Complete list of auth.users FK occurrences"). Also replace the trigger:
- Remove: `CREATE TRIGGER on_auth_user_created AFTER INSERT ON auth.users FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();`
- Replace with: `CREATE TRIGGER on_user_created AFTER INSERT ON public.users FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();`

**Step 5 — Apply Pass 4 (Drop all RLS):**
Remove ALL of the following patterns:
- `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;`
- `CREATE POLICY ... USING (...)` (entire CREATE POLICY statements including multi-line ones ending with `;`)
- Any standalone `auth.uid()` references in policy definitions

**Step 6 — Apply Pass 5 (Replace pgmq calls in check_event_triggers):**
In the content from `20260320000003_event_detector.sql`, replace all three `PERFORM pgmq_public.send(...)` calls with:
```sql
RAISE NOTICE 'RAILWAY: pgmq replaced by BullMQ — event: %', ...;
```
Also remove the `cron.schedule('check-event-triggers', ...)` and `cron.unschedule(...)` block at the bottom of that file's content.

**Step 7 — Apply Pass 6 (Fix pgvector extension schema):**
Replace: `CREATE EXTENSION IF NOT EXISTS vector WITH SCHEMA extensions;`
With: `CREATE EXTENSION IF NOT EXISTS vector;`

**Step 8 — Apply Pass 7 (Drop Supabase role grants):**
Remove lines matching these patterns:
- `GRANT SELECT ON ... TO anon, authenticated;`
- `GRANT SELECT ON ... TO anon;`
- `GRANT SELECT ON ... TO authenticated;`
- `GRANT EXECUTE ON FUNCTION ... TO authenticated;`
- `GRANT EXECUTE ON FUNCTION ... TO service_role;`

Also remove `ALTER PUBLICATION supabase_realtime ADD TABLE ...;` lines.

**Step 9 — Handle backfill script (20260312000004):**
The backfill script references `auth.users` in its INSERT query. Replace:
```sql
INSERT INTO user_agents (...) SELECT ... FROM auth.users ...
```
With a comment:
```sql
-- RAILWAY: backfill from auth.users removed. New users provisioned via API server sign-in handler (Phase 21).
```

**Step 10 — Final sanity grep:**
After writing the file, verify zero occurrences of forbidden patterns by running:
```bash
grep -c 'auth\.users\|auth\.uid\|pgmq\|cron\.schedule\|cron\.unschedule\|vault\.decrypted\|supabase_realtime\|storage\.buckets\|storage\.objects' worrylesssuperagent/RAILWAY_MIGRATION.sql
```
The result MUST be 0. If any matches remain, fix them before considering the task complete.
  </action>
  <verify>
    <automated>cd "/Users/anthonysure/Documents/Worryless AI 33" && grep -c 'auth\.users\|auth\.uid\|pgmq\|cron\.schedule\|cron\.unschedule\|vault\.decrypted\|supabase_realtime\|storage\.buckets\|storage\.objects' worrylesssuperagent/RAILWAY_MIGRATION.sql && echo "FAIL: forbidden patterns found" || echo "PASS: zero forbidden patterns"</automated>
  </verify>
  <acceptance_criteria>
    - `worrylesssuperagent/RAILWAY_MIGRATION.sql` exists and is non-empty
    - `grep -c 'auth\.users' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'auth\.uid' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'pgmq' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'cron\.schedule' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'vault\.decrypted' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'supabase_realtime' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'storage\.buckets\|storage\.objects' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'CREATE TABLE IF NOT EXISTS public.users' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 1
    - `grep -c 'REFERENCES public.users(id)' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns at least 16
    - `grep -c 'CREATE SCHEMA IF NOT EXISTS langgraph' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 1
    - `grep -c 'CREATE EXTENSION IF NOT EXISTS vector;' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 1 (no WITH SCHEMA clause)
    - `grep -c 'ENABLE ROW LEVEL SECURITY' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'CREATE POLICY' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'GRANT.*TO anon' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'GRANT.*TO authenticated' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
    - `grep -c 'on_user_created' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 1 (trigger on public.users)
    - File contains `-- RAILWAY_MIGRATION.sql` header comment
    - File does NOT contain `BEGIN;` or `COMMIT;` transaction wrapper
  </acceptance_criteria>
  <done>RAILWAY_MIGRATION.sql exists with all 34 application tables + public.users + langgraph schema + seed data, zero Supabase-specific references, and passes all grep checks</done>
</task>

</tasks>

<verification>
- `grep -c 'auth\.\|pgmq\|cron\.\|vault\.\|supabase_realtime\|storage\.' worrylesssuperagent/RAILWAY_MIGRATION.sql` returns 0
- File contains CREATE TABLE for all 34 application tables + public.users (35 total)
- File contains CREATE SCHEMA IF NOT EXISTS langgraph
- File contains CREATE EXTENSION IF NOT EXISTS vector (no schema qualifier)
- File contains seed INSERT INTO available_agent_types
</verification>

<success_criteria>
RAILWAY_MIGRATION.sql is a complete, sanitized, flat SQL file ready to apply to Railway Postgres with `psql -f`, containing zero Supabase-specific SQL and all application schema including langgraph and pgvector.
</success_criteria>

<output>
After completion, create `.planning/phases/20-database-migration/20-01-SUMMARY.md`
</output>
