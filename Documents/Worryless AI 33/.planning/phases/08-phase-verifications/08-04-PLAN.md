---
phase: 08-phase-verifications
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-org-view-notifications/05-VERIFICATION.md
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NOTIF-06
  - ORG-01
  - ORG-02
  - ORG-03
  - ORG-04
  - ORG-05

must_haves:
  truths:
    - "05-VERIFICATION.md exists with explicit PASS/FAIL for NOTIF-01..06 and ORG-01..05"
    - "ORG-04 amber dot fix from Phase 6 is confirmed in heartbeatStatus.ts and marked PASS (fixed)"
    - "NOTIF-03 push opt-in surface from Phase 7 is noted with gap-closure accounting and marked PASS"
    - "NOTIF-02 Supabase Realtime subscription confirmed in useNotifications hook"
    - "ORG-03 live pulse animation confirmed in HeartbeatStatusDot component"
  artifacts:
    - path: ".planning/phases/05-org-view-notifications/05-VERIFICATION.md"
      provides: "Formal verification record for Phase 5 Org View + Notifications"
      contains: "PASS/FAIL"
  key_links:
    - from: "src/lib/heartbeatStatus.ts"
      to: "ORG-04"
      via: "getHeartbeatStatus checks urgent||headsup||digest (Phase 6 fix)"
      pattern: "urgent.*headsup.*digest"
    - from: "src/hooks/useNotifications.ts"
      to: "NOTIF-01, NOTIF-02, NOTIF-05, NOTIF-06"
      via: "Realtime subscription + mark-read + resolveView navigation"
      pattern: "supabase.channel|realtime|mark_read"
    - from: "src/components/team/TeamView.tsx"
      to: "ORG-01, ORG-02, ORG-03, ORG-05"
      via: "org chart layout with Chief of Staff + agents + Add Agent"
      pattern: "chief_of_staff|AgentCard|Add Agent"
    - from: "src/hooks/usePushSubscription.ts"
      to: "NOTIF-03"
      via: "VAPID push subscription + Phase 7 opt-in surface"
      pattern: "PushManager|VAPID|subscribe"
---

<objective>
Produce the formal VERIFICATION.md for Phase 5 (Org View + Notifications) by code-reviewing team view components, notification hooks, heartbeat status logic, and push subscription plumbing against the five Phase 5 success criteria and eleven requirements (NOTIF-01..06, ORG-01..05). This verification accounts for two gap closures: the ORG-04 amber dot fix from Phase 6, and the NOTIF-03 push opt-in surface added in Phase 7.

Purpose: Phase 5 has no VERIFICATION.md. The current codebase state includes Phase 6 and Phase 7 fixes. This document records what is true today, including gap-closure accounting.
Output: `.planning/phases/05-org-view-notifications/05-VERIFICATION.md`
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-phase-verifications/08-RESEARCH.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Phase 5 source files and compile evidence</name>
  <files>
    worrylesssuperagent/src/components/team/TeamView.tsx
    worrylesssuperagent/src/components/team/AgentCard.tsx
    worrylesssuperagent/src/lib/heartbeatStatus.ts
    worrylesssuperagent/src/hooks/useNotifications.ts
    worrylesssuperagent/src/hooks/useTeamData.ts
    worrylesssuperagent/src/components/dashboard/NotificationBell.tsx
    worrylesssuperagent/src/hooks/usePushSubscription.ts
    worrylesssuperagent/supabase/migrations/20260313000010_push_subscriptions.sql
    worrylesssuperagent/supabase/migrations/20260313000011_next_digest_run_at.sql
  </files>
  <action>
    Read each file above. Also check for:
    - `src/components/team/HeartbeatStatusDot.tsx` (or inline in AgentCard)
    - `src/components/dashboard/DashboardHeader.tsx` (NotificationBell placement)
    - `src/__tests__/useNotifications.test.ts`
    - `src/__tests__/useTeamData.test.ts`
    - `src/__tests__/usePushSubscription.test.ts`
    - `src/components/onboarding/PushOptInBanner.tsx` or similar (Phase 7 opt-in surface)
    - `src/components/dashboard/Dashboard.tsx` (Phase 7 first-load opt-in banner)

    Evidence to collect per requirement:

    - NOTIF-01: In NotificationBell component, confirm: renders in dashboard header (DashboardHeader), shows unread count badge. Confirm clicking opens a notification panel with entries.
    - NOTIF-02: In useNotifications hook, confirm: Supabase Realtime channel subscription (supabase.channel() call). Confirm the channel is subscribed with a filter on the notifications table (or Broadcast channel). Confirm new notification increments unread count without page refresh (state update in subscription handler).
    - NOTIF-03: In usePushSubscription.ts, confirm VAPID push subscription logic (PushManager.subscribe or similar). Then confirm the Phase 7 opt-in surface exists: (a) PushOptInBanner in onboarding flow (check ConversationalOnboarding or a dedicated Banner component), (b) first-load banner in Dashboard.tsx. Mark NOTIF-03 PASS with note: "VAPID wiring delivered in Phase 5; opt-in surface (PushOptInBanner + Dashboard banner) completed in Phase 7 Plans 03-04. Requirement fully satisfied."
    - NOTIF-04: In heartbeat-runner/index.ts (already read in Plan 08-03 evidence), confirm Resend email is sent for urgent severity. Or re-confirm here: look for Resend import and email send call in runner's urgent branch.
    - NOTIF-05: In useNotifications hook, confirm: (a) mark individual notification read (UPDATE read=true for one notification_id), (b) mark all read (UPDATE read=true for all user's notifications). Confirm unread count updates after mark-read.
    - NOTIF-06: In NotificationBell component or useNotifications hook, confirm resolveView function maps notification's agent_type_id to the correct dashboard view. Confirm clicking a notification entry triggers navigation to the relevant agent panel (setActiveView call or router push).
    - ORG-01: In TeamView.tsx, confirm: (a) Chief of Staff rendered at top (depth=0 or separate tier), (b) all other activated agents rendered as direct reports below, (c) org chart layout (grid or flex with hierarchy).
    - ORG-02: In AgentCard.tsx, confirm each card renders: agent name, role/title, avatar or icon, heartbeat status indicator (HeartbeatStatusDot or inline), last active timestamp, task count for past 7 days.
    - ORG-03: In HeartbeatStatusDot component (or AgentCard), confirm: green pulse animation class when agent heartbeat fired in last hour (status = 'active'), grey/sleeping state, amber/attention state. Check for CSS animation class (pulse, animate-pulse or similar).
    - ORG-04: In heartbeatStatus.ts, confirm the Phase 6 fix: getHeartbeatStatus returns "attention" when lastOutcome is "urgent" OR "headsup" OR "digest" — NOT "surfaced". Extract the exact condition at the relevant line. Mark ORG-04 PASS (fixed Phase 6).
    - ORG-05: In TeamView.tsx, confirm an "Add Agent" button or link is present that opens the Agent Marketplace panel (sets view to 'marketplace' or calls onAddAgent).

    For Phase 6 fix (ORG-04): note "Bug: heartbeatStatus.ts checked lastOutcome === 'surfaced' which was never a real severity value. Fix: now checks urgent || headsup || digest. Phase 6 Plan 02 closed this."
    For Phase 7 gap (NOTIF-03): note the two components that deliver the opt-in surface with file paths.
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Evidence collected for all 11 requirements. ORG-04 Phase 6 fix confirmed. NOTIF-03 Phase 7 opt-in surface confirmed. Vitest suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Write 05-VERIFICATION.md</name>
  <files>.planning/phases/05-org-view-notifications/05-VERIFICATION.md</files>
  <action>
    Write the VERIFICATION.md at `.planning/phases/05-org-view-notifications/05-VERIFICATION.md`.

    **Frontmatter:**
    ```yaml
    ---
    phase: 05-org-view-notifications
    verified_by: Claude (Phase 8 automated code review)
    verified_at: [today's date]
    overall_status: passed
    requirements_verified: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05, NOTIF-06, ORG-01, ORG-02, ORG-03, ORG-04, ORG-05]
    gap_closures_accounted:
      - phase: 06
        fix: heartbeatStatus.ts checks urgent||headsup||digest instead of surfaced
        requirements_affected: [ORG-04]
      - phase: 07
        fix: PushOptInBanner in onboarding + Dashboard first-load banner
        requirements_affected: [NOTIF-03]
    ---
    ```

    **Sections:**

    1. `## Phase 5 Success Criteria` — 5 subsections matching ROADMAP.md Phase 5 criteria verbatim:
       - SC-1: Team view org chart with Chief of Staff + agent cards (name/role/lastActive/taskCount/status)
       - SC-2: Green pulse for recent heartbeat; grey sleeping; amber attention for surfaced findings
       - SC-3: Notification bell with realtime unread count (Supabase Realtime)
       - SC-4: Notification entry navigates to agent panel; mark read / mark all read
       - SC-5: Chief of Staff morning digest at 8am in user timezone
       Each with: Status, Evidence (file + line), Notes.

    2. `## Requirements Map` — Table: Requirement | Status | Evidence | Notes
       For NOTIF-01..06, ORG-01..05.
       For ORG-04: "PASS (fixed Phase 6) — Bug: checked lastOutcome === 'surfaced'. Fix: checks urgent||headsup||digest at heartbeatStatus.ts line N."
       For NOTIF-03: "PASS (gap closed Phase 7) — VAPID wiring in heartbeat-runner confirmed in Phase 5. Opt-in surface (PushOptInBanner + Dashboard first-load banner) added in Phase 7 Plans 03-04. Paths: [components]."

    3. `## Integration Points` — Note confirmed chains:
       - heartbeat-runner → notifications INSERT → DB trigger → Realtime Broadcast → useNotifications state update → NotificationBell unread count
       - AgentCard → heartbeatStatus.ts → getHeartbeatStatus(lastOutcome) → HeartbeatStatusDot rendering
       - notification click → resolveView(agentTypeId) → setActiveView navigation

    4. `## Manual Verification Required` — Items requiring live browser session: Realtime badge increment in real time (requires deployed Supabase + live heartbeat), push notification delivery on mobile PWA (requires VAPID keys + service worker), morning digest at 8am timezone delivery. Include specific steps for each.

    5. `## Sign-Off` block.

    Rules:
    - PASS requires file+line evidence
    - Use "PASS (fixed Phase 6)" for ORG-04, "PASS (gap closed Phase 7)" for NOTIF-03
    - Never mark FAIL for items with confirmed source-level implementations
    - NOTIF-04 (email) can reference evidence from heartbeat-runner; note "evidence confirmed in Plan 08-03 review of heartbeat-runner/index.ts"
  </action>
  <verify>
    <automated>ls /Users/anthonysure/Documents/Worryless\ AI\ 33/.planning/phases/05-org-view-notifications/05-VERIFICATION.md && grep -c "PASS\|FAIL\|MANUAL" /Users/anthonysure/Documents/Worryless\ AI\ 33/.planning/phases/05-org-view-notifications/05-VERIFICATION.md</automated>
  </verify>
  <done>05-VERIFICATION.md exists. Contains PASS/FAIL/MANUAL for all 11 requirements. ORG-04 "PASS (fixed Phase 6)" with heartbeatStatus.ts evidence. NOTIF-03 "PASS (gap closed Phase 7)" with PushOptInBanner file path. Sign-off block present.</done>
</task>

</tasks>

<verification>
1. `.planning/phases/05-org-view-notifications/05-VERIFICATION.md` exists
2. `grep "NOTIF-0[1-6]\|ORG-0[1-5]" .planning/phases/05-org-view-notifications/05-VERIFICATION.md` returns 11+ lines
3. `cd worrylesssuperagent && npx vitest run` — 51 passing, 0 failed
</verification>

<success_criteria>
- 05-VERIFICATION.md exists with correct YAML frontmatter including gap_closures_accounted
- Explicit PASS/FAIL/MANUAL for all 11 requirements
- ORG-04 shows "PASS (fixed Phase 6)" with heartbeatStatus.ts line reference
- NOTIF-03 shows "PASS (gap closed Phase 7)" with PushOptInBanner component path
- NOTIF-02 Realtime subscription confirmed from useNotifications source
- Vitest suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-verifications/08-04-SUMMARY.md`
</output>
