---
phase: 08-phase-verifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-database-foundation/01-VERIFICATION.md
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05
  - DB-06
  - DB-07
  - SEC-01
  - SEC-03

must_haves:
  truths:
    - "01-VERIFICATION.md exists with explicit PASS/FAIL for every DB-01..07, SEC-01, SEC-03 requirement"
    - "Each PASS or FAIL entry cites a specific file path, migration name, or test name as evidence"
    - "Manual-only items (trigger runtime, RLS runtime) are clearly marked MANUAL REQUIRED with test instructions"
    - "Sign-off block contains verifier, timestamp, and overall phase status"
  artifacts:
    - path: ".planning/phases/01-database-foundation/01-VERIFICATION.md"
      provides: "Formal verification record for Phase 1 Database Foundation"
      contains: "PASS/FAIL"
  key_links:
    - from: "supabase/migrations/20260312000001_create_agent_tables.sql"
      to: "DB-01, DB-02, DB-03, DB-05, DB-06"
      via: "schema review"
      pattern: "UNIQUE.*user_id.*agent_type"
    - from: "supabase/migrations/20260312000002_workspace_trigger.sql"
      to: "DB-04"
      via: "trigger code review"
      pattern: "CREATE TRIGGER on_agent_activated"
    - from: "supabase/functions/_shared/sanitize.ts"
      to: "SEC-03"
      via: "pattern list review"
      pattern: "IGNORE PREVIOUS INSTRUCTIONS"
    - from: "supabase/functions/planning-agent/index.ts"
      to: "SEC-01"
      via: "JWT extraction pattern"
      pattern: "auth.getUser"
---

<objective>
Produce the formal VERIFICATION.md for Phase 1 (Database Foundation) by code-reviewing all migration files, trigger SQL, seed data, RLS policies, and security-hardened edge functions against the five Phase 1 success criteria and nine requirements (DB-01..07, SEC-01, SEC-03).

Purpose: Close the verification gap identified in the milestone audit — Phase 1 has complete implementation but no VERIFICATION.md sign-off. This record is required before milestone sign-off.
Output: `.planning/phases/01-database-foundation/01-VERIFICATION.md` with explicit PASS/FAIL per requirement, file-path evidence, and MANUAL REQUIRED markers for live-DB-only tests.
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-phase-verifications/08-RESEARCH.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Phase 1 source files and compile evidence</name>
  <files>
    worrylesssuperagent/supabase/migrations/20260312000001_create_agent_tables.sql
    worrylesssuperagent/supabase/migrations/20260312000002_workspace_trigger.sql
    worrylesssuperagent/supabase/migrations/20260312000003_seed_agent_types.sql
    worrylesssuperagent/supabase/migrations/20260312000004_backfill_existing_users.sql
    worrylesssuperagent/supabase/functions/_shared/sanitize.ts
    worrylesssuperagent/supabase/functions/planning-agent/index.ts
    worrylesssuperagent/supabase/functions/generate-leads/index.ts
    worrylesssuperagent/supabase/functions/crawl-business-website/index.ts
  </files>
  <action>
    Read each of the eight source files listed above. For each file, extract the evidence needed to make a PASS/FAIL determination for the requirements listed below. Do NOT rely on SUMMARY.md frontmatter — read the actual files.

    Evidence to collect per requirement:
    - DB-01: Count rows in seed_agent_types migration; confirm 13 rows (Chief of Staff + 12 specialists) with all 6 default MD workspace template columns and skill_config column present
    - DB-02: Confirm user_agents table in create_agent_tables.sql has heartbeat config columns: heartbeat_interval, active_hours_start, active_hours_end, heartbeat_enabled
    - DB-03: Confirm agent_workspaces table has file_type column; confirm valid values include the 6 types (IDENTITY, SOUL, SOPs, MEMORY, HEARTBEAT, TOOLS) — these are seeded via trigger not constrained as ENUM in schema
    - DB-04: Confirm trigger `on_agent_activated` exists in workspace_trigger.sql, fires AFTER INSERT ON user_agents, calls create_agent_workspace(), and creates exactly 6 rows
    - DB-05: Confirm agent_heartbeat_log table; confirm no INSERT policy for authenticated users (service role only); confirm only surfaced/error rows are written by checking the comment or trigger code that suppresses ok runs
    - DB-06: Confirm UNIQUE constraint on user_agents(user_id, agent_type_id) exists in create_agent_tables.sql
    - DB-07: profiles.timezone — this was confirmed by Phase 1 decisions to be in migration 20251216134813; read the SUMMARY or note it as MANUAL REQUIRED if migration is not in the 20260312 series
    - SEC-01: In each of the three edge functions (planning-agent, generate-leads, crawl-business-website), confirm: (a) Authorization header is read, (b) auth.getUser(token) is called, (c) userId is derived from user.id (JWT), NOT from req.body or query params
    - SEC-03: In sanitize.ts, confirm: injection patterns list includes at minimum "IGNORE PREVIOUS INSTRUCTIONS" and `<system>` tag; confirm exported sanitize function is used

    Record: file path + line number (or line range) for every piece of evidence. Note any item that cannot be confirmed from code alone as MANUAL REQUIRED.
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Evidence table exists in working memory for all 9 requirements, with file path + line references. Vitest suite still passing (no regressions from reading).</done>
</task>

<task type="auto">
  <name>Task 2: Write 01-VERIFICATION.md</name>
  <files>.planning/phases/01-database-foundation/01-VERIFICATION.md</files>
  <action>
    Write the VERIFICATION.md document at `.planning/phases/01-database-foundation/01-VERIFICATION.md`. Use the evidence gathered in Task 1.

    Structure the document exactly as follows:

    **Frontmatter (YAML):**
    ```yaml
    ---
    phase: 01-database-foundation
    verified_by: Claude (Phase 8 automated code review)
    verified_at: [today's date]
    overall_status: passed   # or partial if MANUAL items exist
    requirements_verified: [DB-01, DB-02, DB-03, DB-04, DB-05, DB-06, DB-07, SEC-01, SEC-03]
    ---
    ```

    **Sections to include:**

    1. `## Phase 1 Success Criteria` — For each of the 5 success criteria from ROADMAP.md Phase 1, one subsection:
       - Status: PASS / FAIL / MANUAL REQUIRED
       - Evidence: file path + line reference or SQL construct cited
       - Notes: any caveats (e.g., gap-closure, migration order)

    2. `## Requirements Map` — Markdown table:
       | Requirement | Status | Evidence | Notes |
       For each of DB-01..07, SEC-01, SEC-03.

    3. `## Integration Points` — Paragraph noting cross-phase wiring confirmed: trigger auto-populates workspaces (used in Phase 2, 3), sanitize.ts mirrored in src/lib/sanitize.ts (used in Phase 3)

    4. `## Manual Verification Required` — Table listing any items that require a live Supabase instance, with specific SQL or test instructions the human verifier must run. At minimum include: DB-04 (trigger runtime), DB-05 (RLS cross-user isolation), SEC-01 (live 401 response).

    5. `## Sign-Off`
       ```
       - Verifier: Claude (Phase 8 automated code review)
       - Date: [today's date]
       - Vitest suite: 51 passing, 0 failed (confirmed 2026-03-17)
       - Manual items pending human sign-off: [list]
       ```

    Rules:
    - PASS requires a specific file path and line reference as evidence
    - FAIL requires a description of what was expected vs. what was found
    - MANUAL REQUIRED must include specific test SQL or browser steps
    - Never mark FAIL simply because a live DB test cannot be run in vitest
    - DB-07 (profiles.timezone): if the migration is in a pre-existing migration file (20251216134813), cite it from the SUMMARY decisions notes and mark it PASS with note "migration predates Phase 1 series; confirmed in Phase 1 decision log"
  </action>
  <verify>
    <automated>ls /Users/anthonysure/Documents/Worryless\ AI\ 33/.planning/phases/01-database-foundation/01-VERIFICATION.md && grep -c "PASS\|FAIL\|MANUAL" /Users/anthonysure/Documents/Worryless\ AI\ 33/.planning/phases/01-database-foundation/01-VERIFICATION.md</automated>
  </verify>
  <done>01-VERIFICATION.md exists at the correct path. File contains explicit PASS/FAIL/MANUAL REQUIRED for all 9 requirements (DB-01..07, SEC-01, SEC-03). Every PASS cites a file path. Sign-off block present.</done>
</task>

</tasks>

<verification>
1. `.planning/phases/01-database-foundation/01-VERIFICATION.md` exists
2. `grep "DB-01\|DB-02\|DB-03\|DB-04\|DB-05\|DB-06\|DB-07\|SEC-01\|SEC-03" .planning/phases/01-database-foundation/01-VERIFICATION.md` returns 9+ lines
3. `cd worrylesssuperagent && npx vitest run` — 51 passing, 0 failed (no regressions)
</verification>

<success_criteria>
- 01-VERIFICATION.md exists with correct YAML frontmatter
- Explicit PASS/FAIL/MANUAL REQUIRED for all 9 Phase 1 requirements
- Every PASS entry cites a specific file path + line or SQL construct
- Manual-only items documented with test instructions
- Vitest suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-verifications/08-01-SUMMARY.md`
</output>
