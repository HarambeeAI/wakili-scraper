---
phase: 17-generative-ui-onboarding-redesign
plan: 04
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - worrylesssuperagent/src/components/ui/InlinePLTable.tsx
  - worrylesssuperagent/src/components/ui/PipelineKanban.tsx
  - worrylesssuperagent/src/components/ui/ContentCalendarGrid.tsx
  - worrylesssuperagent/src/components/ui/InvoiceTrackerTable.tsx
  - worrylesssuperagent/src/components/ui/CalendarTimelineView.tsx
  - worrylesssuperagent/src/components/ui/MeetingBriefCard.tsx
  - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx
autonomous: true
requirements: [GUI-07]
must_haves:
  truths:
    - "PipelineKanban renders deal columns (New, Contacted, Qualified, Proposal, Won, Lost)"
    - "ContentCalendarGrid renders a weekly grid of scheduled posts"
    - "InlinePLTable renders a P&L table with revenue and expense categories"
    - "InvoiceTrackerTable renders invoices with status badges"
    - "CalendarTimelineView renders day events in time order"
    - "MeetingBriefCard renders attendees, agenda, and linked documents"
    - "GenerativeUIRenderer dispatches to all 6 new domain components instead of placeholders"
  artifacts:
    - path: "worrylesssuperagent/src/components/ui/PipelineKanban.tsx"
      provides: "Deal pipeline board with status columns"
      exports: ["PipelineKanban"]
    - path: "worrylesssuperagent/src/components/ui/ContentCalendarGrid.tsx"
      provides: "Weekly post schedule grid"
      exports: ["ContentCalendarGrid"]
    - path: "worrylesssuperagent/src/components/ui/InlinePLTable.tsx"
      provides: "P&L table with MoM comparison"
      exports: ["InlinePLTable"]
    - path: "worrylesssuperagent/src/components/ui/InvoiceTrackerTable.tsx"
      provides: "Invoice list with status badges"
      exports: ["InvoiceTrackerTable"]
    - path: "worrylesssuperagent/src/components/ui/CalendarTimelineView.tsx"
      provides: "Day-view event timeline"
      exports: ["CalendarTimelineView"]
    - path: "worrylesssuperagent/src/components/ui/MeetingBriefCard.tsx"
      provides: "Meeting brief with attendees, agenda, docs"
      exports: ["MeetingBriefCard"]
  key_links:
    - from: "worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx"
      to: "worrylesssuperagent/src/components/ui/PipelineKanban.tsx"
      via: "import for pipeline_kanban case"
      pattern: "PipelineKanban"
---

<objective>
Build the 6 domain-specific generative UI components (PipelineKanban, ContentCalendarGrid, InlinePLTable, InvoiceTrackerTable, CalendarTimelineView, MeetingBriefCard) and wire them into GenerativeUIRenderer replacing the placeholder divs.

Purpose: These components make the chat interface truly useful for each agent domain — the Accountant can show real P&L tables, Sales Rep can show pipeline boards, Marketer can show content calendars, PA can show day schedules.

Output: 6 domain components + updated GenerativeUIRenderer dispatch.
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-generative-ui-onboarding-redesign/17-UI-SPEC.md
@.planning/phases/17-generative-ui-onboarding-redesign/17-02-SUMMARY.md

<interfaces>
<!-- DataTable from Plan 02 -->
```typescript
export function DataTable({ data, columns }: { data: Record<string, unknown>[]; columns: { key: string; label: string }[] }): JSX.Element;
```

<!-- GenerativeUIRenderer switch cases needing replacement -->
<!-- pipeline_kanban, content_calendar, calendar_timeline, meeting_brief, dynamic_form are placeholders -->
<!-- pl_report and invoice_tracker already use DataTable but can be upgraded to domain components -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 6 domain-specific components</name>
  <files>worrylesssuperagent/src/components/ui/InlinePLTable.tsx, worrylesssuperagent/src/components/ui/PipelineKanban.tsx, worrylesssuperagent/src/components/ui/ContentCalendarGrid.tsx, worrylesssuperagent/src/components/ui/InvoiceTrackerTable.tsx, worrylesssuperagent/src/components/ui/CalendarTimelineView.tsx, worrylesssuperagent/src/components/ui/MeetingBriefCard.tsx</files>
  <read_first>
    - worrylesssuperagent/src/components/ui/DataTable.tsx (base table component from Plan 02)
    - worrylesssuperagent/src/components/ui/card.tsx (Card, CardContent, CardHeader, CardTitle)
    - worrylesssuperagent/src/components/ui/badge.tsx (Badge for status)
    - worrylesssuperagent/src/components/ui/scroll-area.tsx (ScrollArea)
    - .planning/phases/17-generative-ui-onboarding-redesign/17-UI-SPEC.md (component specs)
  </read_first>
  <action>
    1. Create `worrylesssuperagent/src/components/ui/InlinePLTable.tsx`:
       Props: `{ title?: string; rows: { category: string; current: number; previous: number; change: number }[]; period?: string }`
       - Header row: Category, Current Period, Previous Period, Change (%)
       - Use DataTable internally with columns mapped from props
       - Revenue rows: green text for positive change, red for negative
       - Bottom row: Net Income (bold, border-top)
       - Empty state: "No P&L data available"
       - Export: `export function InlinePLTable`

    2. Create `worrylesssuperagent/src/components/ui/PipelineKanban.tsx`:
       Props: `{ deals: { id: string; name: string; value: number; status: string; company?: string }[]; }`
       - 6 columns: New, Contacted, Qualified, Proposal, Won, Lost
       - Each column: `<div className="min-w-[160px] bg-muted/30 rounded-lg p-2">` with column header showing count
       - Each deal card: `<Card className="p-2 mb-2">` with deal name (14px semibold), company (12px muted), value formatted as currency
       - Horizontal scroll wrapper: `<ScrollArea className="w-full"><div className="flex gap-3 min-w-max pb-2">...</div></ScrollArea>`
       - Empty state per UI-SPEC: "No deals in pipeline. Ask your Sales Rep to generate leads."
       - Export: `export function PipelineKanban`

    3. Create `worrylesssuperagent/src/components/ui/ContentCalendarGrid.tsx`:
       Props: `{ posts: { id: string; title: string; platform: string; date: string; status: string }[]; weekStart?: string }`
       - 7 columns (Mon-Sun), group posts by day of week
       - Each cell: date header (12px semibold), post cards below
       - Post card: `<div className="text-xs p-1.5 rounded bg-card border mb-1">` with platform badge + title truncated
       - Platform badges: Instagram (pink), Twitter/X (blue), LinkedIn (indigo), TikTok (cyan)
       - Empty state per UI-SPEC: "No scheduled posts. Ask your Marketer to create a content plan."
       - Export: `export function ContentCalendarGrid`

    4. Create `worrylesssuperagent/src/components/ui/InvoiceTrackerTable.tsx`:
       Props: `{ invoices: { id: string; client: string; amount: number; status: string; due_date: string; invoice_number?: string }[] }`
       - Use DataTable with columns: Invoice #, Client, Amount, Due Date, Status
       - Status badges: `paid` = green Badge, `overdue` = red Badge, `sent` = amber Badge, `draft` = gray Badge
       - Amount formatted with currency: `$${amount.toLocaleString()}`
       - Export: `export function InvoiceTrackerTable`

    5. Create `worrylesssuperagent/src/components/ui/CalendarTimelineView.tsx`:
       Props: `{ events: { id: string; title: string; start: string; end: string; type?: string; location?: string }[]; date?: string }`
       - Vertical timeline with time markers on left
       - Each event: `<div className="flex gap-3 py-2 border-b border-border last:border-0">` with:
         - Time: `<span className="text-xs text-muted-foreground w-16 shrink-0">{formatTime(start)}-{formatTime(end)}</span>`
         - Content: title (14px), location (12px muted) if present
         - Type badge: "meeting" (blue), "focus" (green), "travel" (amber), default (gray)
       - Events sorted by start time
       - Empty state: "No events scheduled for this day."
       - Export: `export function CalendarTimelineView`

    6. Create `worrylesssuperagent/src/components/ui/MeetingBriefCard.tsx`:
       Props: `{ meeting: { title: string; date: string; time: string; attendees: { name: string; role?: string }[]; agenda: string[]; documents?: { name: string; url: string }[] } }`
       - Card layout with sections:
         - Header: meeting title (20px semibold), date/time (12px muted)
         - Attendees section: `<h4 className="text-xs font-semibold uppercase text-muted-foreground mb-2">Attendees</h4>` followed by attendee list with name + role
         - Agenda section: ordered list `<ol className="list-decimal list-inside text-sm space-y-1">`
         - Documents section (if present): linked document names with FileText icon
       - Export: `export function MeetingBriefCard`
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/ --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - worrylesssuperagent/src/components/ui/InlinePLTable.tsx contains `export function InlinePLTable`
    - worrylesssuperagent/src/components/ui/PipelineKanban.tsx contains `export function PipelineKanban`
    - worrylesssuperagent/src/components/ui/PipelineKanban.tsx contains `No deals in pipeline`
    - worrylesssuperagent/src/components/ui/ContentCalendarGrid.tsx contains `export function ContentCalendarGrid`
    - worrylesssuperagent/src/components/ui/ContentCalendarGrid.tsx contains `No scheduled posts`
    - worrylesssuperagent/src/components/ui/InvoiceTrackerTable.tsx contains `export function InvoiceTrackerTable`
    - worrylesssuperagent/src/components/ui/CalendarTimelineView.tsx contains `export function CalendarTimelineView`
    - worrylesssuperagent/src/components/ui/MeetingBriefCard.tsx contains `export function MeetingBriefCard`
    - Existing test suite still passes
  </acceptance_criteria>
  <done>All 6 domain components built with proper empty states and data rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Wire domain components into GenerativeUIRenderer — replace placeholders</name>
  <files>worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx</files>
  <read_first>
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx (current dispatcher with placeholder divs)
    - worrylesssuperagent/src/components/ui/InlinePLTable.tsx (just created)
    - worrylesssuperagent/src/components/ui/PipelineKanban.tsx (just created)
    - worrylesssuperagent/src/components/ui/ContentCalendarGrid.tsx (just created)
    - worrylesssuperagent/src/components/ui/InvoiceTrackerTable.tsx (just created)
    - worrylesssuperagent/src/components/ui/CalendarTimelineView.tsx (just created)
    - worrylesssuperagent/src/components/ui/MeetingBriefCard.tsx (just created)
  </read_first>
  <action>
    1. Add imports to GenerativeUIRenderer.tsx:
       ```typescript
       import { InlinePLTable } from "@/components/ui/InlinePLTable";
       import { PipelineKanban } from "@/components/ui/PipelineKanban";
       import { ContentCalendarGrid } from "@/components/ui/ContentCalendarGrid";
       import { InvoiceTrackerTable } from "@/components/ui/InvoiceTrackerTable";
       import { CalendarTimelineView } from "@/components/ui/CalendarTimelineView";
       import { MeetingBriefCard } from "@/components/ui/MeetingBriefCard";
       ```

    2. Replace placeholder switch cases:
       - `"pl_report"`: `<InlinePLTable title={comp.props.title as string} rows={comp.props.rows as any[]} period={comp.props.period as string} />`
       - `"pipeline_kanban"`: `<PipelineKanban deals={comp.props.deals as any[]} />`
       - `"content_calendar"`: `<ContentCalendarGrid posts={comp.props.posts as any[]} weekStart={comp.props.weekStart as string} />`
       - `"invoice_tracker"`: `<InvoiceTrackerTable invoices={comp.props.invoices as any[]} />`
       - `"calendar_timeline"`: `<CalendarTimelineView events={comp.props.events as any[]} date={comp.props.date as string} />`
       - `"meeting_brief"`: `<MeetingBriefCard meeting={comp.props.meeting as any} />`
       - `"dynamic_form"`: Leave as placeholder for now (forms are less critical than data display)

    3. Verify the GenerativeUIRenderer test still passes (chart mocks should not conflict with new imports since the new components are not chart-based).
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/GenerativeUIRenderer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx contains `import { InlinePLTable }`
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx contains `import { PipelineKanban }`
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx contains `import { ContentCalendarGrid }`
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx contains `import { InvoiceTrackerTable }`
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx contains `import { CalendarTimelineView }`
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx contains `import { MeetingBriefCard }`
    - worrylesssuperagent/src/components/chat/GenerativeUIRenderer.tsx does NOT contain `Plan 04` placeholder text (except for dynamic_form)
    - GenerativeUIRenderer test passes
  </acceptance_criteria>
  <done>GenerativeUIRenderer dispatches all 13 component types to real implementations (12 full + 1 dynamic_form placeholder).</done>
</task>

</tasks>

<verification>
- `cd worrylesssuperagent && npx vitest run src/__tests__/GenerativeUIRenderer.test.ts` passes
- `cd worrylesssuperagent && npx vitest run` full suite passes
- All 6 domain component files exist with correct exports
</verification>

<success_criteria>
- All 6 domain components render data from props with proper empty states
- GenerativeUIRenderer dispatches to real components instead of placeholders
- Existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-generative-ui-onboarding-redesign/17-04-SUMMARY.md`
</output>
