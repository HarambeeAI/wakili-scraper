---
phase: 04-heartbeat-system
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - worrylesssuperagent/src/__tests__/heartbeatParser.test.ts
  - worrylesssuperagent/src/__tests__/heartbeatDispatcher.test.ts
  - worrylesssuperagent/src/__tests__/useHeartbeatConfig.test.ts
autonomous: true
requirements:
  - HB-03
  - HB-05
  - HB-06
  - HB-08
  - SEC-02
must_haves:
  truths:
    - "Running `npx vitest run src/__tests__/` exits 0 before any implementation exists"
    - "All three test files have it.todo stubs for every required behavior"
    - "No test file imports a non-existent module (stubs only)"
  artifacts:
    - path: "worrylesssuperagent/src/__tests__/heartbeatParser.test.ts"
      provides: "Stubs for severity parse, extractJson strip-fences, fail-safe to ok"
    - path: "worrylesssuperagent/src/__tests__/heartbeatDispatcher.test.ts"
      provides: "Stubs for budget count, active hours, SEC-02 no-body-userId"
    - path: "worrylesssuperagent/src/__tests__/useHeartbeatConfig.test.ts"
      provides: "Stubs for hook reads/writes user_agents heartbeat columns"
  key_links:
    - from: "heartbeatParser.test.ts"
      to: "heartbeat-runner/index.ts (future)"
      via: "it.todo stubs document expected behavior contracts"
      pattern: "it\\.todo"
---

<objective>
Create Wave 0 test scaffolds for Phase 4 per the Nyquist rule — all automated verify commands must reference existing test files before implementation tasks run.

Purpose: Unblock Wave 1+ plans by ensuring every `<automated>` verify command points to a real file that vitest can discover. Tests are `it.todo` stubs that exit 0 now and become real tests as implementations land.
Output: Three test files in `src/__tests__/` covering HB-03 (parser), HB-05/HB-06/SEC-02 (dispatcher logic), and HB-08 (config hook).
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-heartbeat-system/04-VALIDATION.md

Existing test infrastructure: `worrylesssuperagent/vitest.config.ts` (already configured, excludes supabase/ dir)
Pattern reference: Phase 3 used `it.todo` stubs in `useWorkspaceAutoSave.test.ts` so vitest exits 0 without the hook existing — mirror that pattern here.
Quick run: `cd worrylesssuperagent && npx vitest run src/__tests__/`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold heartbeatParser.test.ts and heartbeatDispatcher.test.ts</name>
  <files>worrylesssuperagent/src/__tests__/heartbeatParser.test.ts, worrylesssuperagent/src/__tests__/heartbeatDispatcher.test.ts</files>
  <behavior>
    heartbeatParser.test.ts stubs:
    - it.todo "severity: extractJson strips markdown code fences before JSON.parse"
    - it.todo "severity: returns parsed severity from valid JSON string"
    - it.todo "severity: defaults to ok on malformed JSON (fail-safe)"
    - it.todo "severity: defaults to ok when severity field is missing"

    heartbeatDispatcher.test.ts stubs:
    - it.todo "budget: returns true (skip) when heartbeat_log count >= daily budget"
    - it.todo "budget: returns false (allow) when count is below budget"
    - it.todo "activeHours: returns false for UTC time that falls outside user local window"
    - it.todo "activeHours: returns true for UTC time within user local window"
    - it.todo "SEC-02: dispatcher reads userId from user_agents DB row, not from request body"
  </behavior>
  <action>
    Create `src/__tests__/heartbeatParser.test.ts`:
    ```typescript
    import { describe, it } from 'vitest';

    describe('heartbeatParser', () => {
      describe('extractJson', () => {
        it.todo('strips markdown code fences before JSON.parse');
        it.todo('passes through clean JSON unchanged');
      });
      describe('severity', () => {
        it.todo('returns parsed severity from valid JSON string');
        it.todo('defaults to "ok" on malformed JSON (fail-safe)');
        it.todo('defaults to "ok" when severity field is missing');
        it.todo('accepts all four valid severity values: urgent, headsup, digest, ok');
      });
    });
    ```

    Create `src/__tests__/heartbeatDispatcher.test.ts`:
    ```typescript
    import { describe, it } from 'vitest';

    describe('heartbeatDispatcher', () => {
      describe('budget', () => {
        it.todo('returns true (skip) when heartbeat_log count >= daily budget');
        it.todo('returns false (allow) when count is below budget');
        it.todo('uses default budget of 6 when heartbeat_daily_budget is null');
      });
      describe('activeHours', () => {
        it.todo('returns false for UTC time that falls outside user local active window');
        it.todo('returns true for UTC time within user local active window');
        it.todo('defaults to UTC timezone when profiles.timezone is null');
      });
      describe('SEC-02', () => {
        it.todo('dispatcher reads userId from user_agents DB row, never from request body');
        it.todo('request body fields are ignored when determining user identity');
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/heartbeatParser.test.ts src/__tests__/heartbeatDispatcher.test.ts</automated>
  </verify>
  <done>Both files exist, vitest reports 0 failed (all todo), exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold useHeartbeatConfig.test.ts</name>
  <files>worrylesssuperagent/src/__tests__/useHeartbeatConfig.test.ts</files>
  <behavior>
    Stubs for HB-08 hook behavior:
    - it.todo "reads heartbeat_interval_hours from user_agents row for the given agentTypeId"
    - it.todo "reads heartbeat_enabled from user_agents row"
    - it.todo "reads heartbeat_active_hours_start and heartbeat_active_hours_end"
    - it.todo "updateConfig PATCHes correct user_agents row (not a different user's row)"
    - it.todo "isSaving is true during the update call"
  </behavior>
  <action>
    Create `src/__tests__/useHeartbeatConfig.test.ts`:
    ```typescript
    import { describe, it } from 'vitest';

    describe('useHeartbeatConfig', () => {
      describe('read', () => {
        it.todo('reads heartbeat_interval_hours from user_agents for given agentTypeId');
        it.todo('reads heartbeat_enabled from user_agents');
        it.todo('reads heartbeat_active_hours_start and heartbeat_active_hours_end');
        it.todo('returns null config while loading');
      });
      describe('updateConfig', () => {
        it.todo('PATCHes the correct user_agents row (matching user_id + agent_type_id)');
        it.todo('sets isSaving true during update, false after completion');
        it.todo('accepts partial update (only changed fields)');
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/</automated>
  </verify>
  <done>All three test files exist, `npx vitest run src/__tests__/` exits 0 with all tests as todo</done>
</task>

</tasks>

<verification>
`cd worrylesssuperagent && npx vitest run src/__tests__/` exits 0 before any implementation. All three test files in place. No import errors.
</verification>

<success_criteria>
- `src/__tests__/heartbeatParser.test.ts` exists with describe/it.todo stubs
- `src/__tests__/heartbeatDispatcher.test.ts` exists with describe/it.todo stubs
- `src/__tests__/useHeartbeatConfig.test.ts` exists with describe/it.todo stubs
- `npx vitest run src/__tests__/` exits 0
- Wave 0 complete flag can be set to true in 04-VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/04-heartbeat-system/04-01-SUMMARY.md`
</output>
