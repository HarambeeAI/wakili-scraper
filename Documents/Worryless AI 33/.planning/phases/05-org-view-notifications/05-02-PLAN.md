---
phase: 05-org-view-notifications
plan: "02"
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - worrylesssuperagent/src/hooks/useNotifications.ts
  - worrylesssuperagent/src/components/dashboard/NotificationBell.tsx
  - worrylesssuperagent/src/components/dashboard/DashboardHeader.tsx
  - worrylesssuperagent/src/pages/Dashboard.tsx
  - worrylesssuperagent/src/__tests__/useNotifications.test.ts
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-05, NOTIF-06]

must_haves:
  truths:
    - "Dashboard header shows a bell icon with a red badge when unread notifications exist"
    - "Unread count updates in real time without page refresh when a new notification row is inserted"
    - "Clicking the bell opens a popover panel listing notifications with severity labels"
    - "User can mark individual notifications as read — unread count decrements immediately (optimistic)"
    - "User can mark all as read — unread count drops to 0 immediately"
    - "Clicking a notification navigates to the correct agent panel (uses LEGACY_VIEW_MAP resolution)"
  artifacts:
    - path: "worrylesssuperagent/src/hooks/useNotifications.ts"
      provides: "React hook: initial unread count query + postgres_changes subscription + markRead + markAllRead"
      exports: ["useNotifications"]
    - path: "worrylesssuperagent/src/components/dashboard/NotificationBell.tsx"
      provides: "Bell button with badge + Radix Popover panel + notification list + mark read controls"
      exports: ["NotificationBell"]
    - path: "worrylesssuperagent/src/components/dashboard/DashboardHeader.tsx"
      provides: "Header with NotificationBell wired to user.id and onNavigate"
  key_links:
    - from: "NotificationBell.tsx"
      to: "useNotifications"
      via: "hook call with userId"
      pattern: "useNotifications\\(userId\\)"
    - from: "useNotifications.ts"
      to: "supabase notifications table"
      via: "postgres_changes subscription with filter user_id=eq.userId"
      pattern: "postgres_changes"
    - from: "notification click"
      to: "agent panel view"
      via: "LEGACY_VIEW_MAP[agentTypeId] || 'agent:'+agentTypeId then onNavigate"
      pattern: "LEGACY_VIEW_MAP"
---

<objective>
Build the notification bell with real-time unread count, notification panel, mark-read controls, and agent navigation links.

Purpose: Users see alerts from heartbeat findings without refreshing; urgent findings are immediately visible as a red badge on the bell.
Output: `useNotifications` hook + `NotificationBell` component wired into `DashboardHeader`.
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-org-view-notifications/05-RESEARCH.md

@worrylesssuperagent/src/pages/Dashboard.tsx
@worrylesssuperagent/src/components/dashboard/DashboardHeader.tsx
@worrylesssuperagent/src/components/dashboard/DashboardSidebar.tsx
@worrylesssuperagent/src/__tests__/useNotifications.test.ts

<interfaces>
<!-- notifications table schema — from 20260313000006_heartbeat_queue.sql -->
-- notifications (
--   id UUID PK,
--   user_id UUID,
--   agent_type_id TEXT,
--   severity TEXT CHECK ('urgent','headsup','digest'),
--   message TEXT,
--   is_read BOOLEAN DEFAULT false,
--   link_type TEXT,          -- 'agent_panel'
--   created_at TIMESTAMPTZ
-- )
-- RLS: SELECT + UPDATE WHERE auth.uid() = user_id (already exists from Phase 4)

<!-- Dashboard.tsx ActiveView type -->
export type ActiveView =
  | "overview"
  | "accountant"
  | "marketer"
  | "sales"
  | "assistant"
  | "chat"
  | "settings"
  | "artifacts"
  | "marketplace"
  | string;

<!-- DashboardSidebar.tsx LEGACY_VIEW_MAP -->
const LEGACY_VIEW_MAP: Record<string, string> = {
  chief_of_staff: "chat",
  personal_assistant: "assistant",
  accountant: "accountant",
  marketer: "marketer",
  sales_rep: "sales",
};

<!-- DashboardHeader current shape -->
interface DashboardHeaderProps {
  user: User;
}
export function DashboardHeader({ user }: DashboardHeaderProps)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useNotifications hook</name>
  <files>worrylesssuperagent/src/hooks/useNotifications.ts, worrylesssuperagent/src/__tests__/useNotifications.test.ts</files>
  <behavior>
    - initial count: on mount with userId, queries notifications where is_read=false, sets unreadCount to the result
    - realtime: a postgres_changes INSERT subscription on table notifications filtered to user_id increments unreadCount by 1
    - markRead: sets unreadCount = max(0, prev-1) immediately, then calls supabase update is_read=true for the given id
    - markAllRead: sets unreadCount = 0 immediately, then calls supabase update is_read=true for all user's unread rows
    - navigation: resolveView(agentTypeId) applies LEGACY_VIEW_MAP[agentTypeId] if present, otherwise 'agent:'+agentTypeId
    - cleanup: useEffect returns () => { supabase.removeChannel(channel) } to prevent channel leak
  </behavior>
  <action>
    Create `worrylesssuperagent/src/hooks/useNotifications.ts`.

    Hook signature:
    ```typescript
    export function useNotifications(userId: string | undefined) {
      // returns: { unreadCount, notifications, markRead, markAllRead, resolveView }
    }
    ```

    Implementation:
    - useEffect([userId]): if !userId return; query .from('notifications').select('*').eq('user_id',userId).eq('is_read',false).order('created_at',{ascending:false}) to populate notifications list AND .select('id',{count:'exact',head:true}) for initial unreadCount
    - Realtime: subscribe to channel `notifications:${userId}` with postgres_changes INSERT filter `user_id=eq.${userId}`; on event: setUnreadCount(prev=>prev+1) and setNotifications(prev=>[newRow, ...prev])
    - markRead(id: string): setUnreadCount(prev=>Math.max(0,prev-1)); setNotifications(prev=>prev.map(n=>n.id===id?{...n,is_read:true}:n)); await supabase.from('notifications').update({is_read:true}).eq('id',id).eq('user_id',userId)
    - markAllRead(): setUnreadCount(0); setNotifications(prev=>prev.map(n=>({...n,is_read:true}))); await supabase.from('notifications').update({is_read:true}).eq('user_id',userId).eq('is_read',false)
    - resolveView(agentTypeId: string): const map: Record<string,string> = {chief_of_staff:'chat',personal_assistant:'assistant',accountant:'accountant',marketer:'marketer',sales_rep:'sales'}; return map[agentTypeId] ?? `agent:${agentTypeId}`
    - Cast supabase queries as any (notifications table not in generated types — same pattern as useAgentWorkspace)
    - Cleanup: supabase.removeChannel(channel) on unmount

    Update `src/__tests__/useNotifications.test.ts`: replace it.todo stubs with real tests using vi.mock('@/integrations/supabase/client'). Test resolveView directly (pure logic, no mock needed). Test optimistic updates to unreadCount state.
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/useNotifications.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All useNotifications tests pass. resolveView returns 'chat' for 'chief_of_staff', 'agent:hr_manager' for 'hr_manager'. Hook file exists.</done>
</task>

<task type="auto">
  <name>Task 2: NotificationBell component + DashboardHeader wiring</name>
  <files>worrylesssuperagent/src/components/dashboard/NotificationBell.tsx, worrylesssuperagent/src/components/dashboard/DashboardHeader.tsx, worrylesssuperagent/src/pages/Dashboard.tsx</files>
  <action>
    Create `worrylesssuperagent/src/components/dashboard/NotificationBell.tsx`.

    Props:
    ```typescript
    interface NotificationBellProps {
      userId: string | undefined;
      onNavigate: (view: string) => void;
    }
    ```

    Component structure (use code example from RESEARCH.md exactly):
    - Radix Popover anchored to a ghost Button with Bell (or BellDot when unreadCount > 0) icon from lucide-react
    - Badge: absolute positioned `<span>` with bg-destructive, shows unreadCount (99+ if over 99), hidden when unreadCount === 0
    - PopoverContent (align="end", className="w-80 p-0"):
      - Header row: "Notifications" text + "Mark all read" Button (variant=ghost, size=sm) — calls markAllRead(); disabled when unreadCount===0
      - ScrollArea (max-h-96): maps over notifications array
        - Each row: clicking calls onNavigate(resolveView(notification.agent_type_id)); shows agent_type_id label, severity badge (destructive for urgent, default for headsup, secondary for digest), message truncated to 2 lines, formatDistanceToNow(created_at) from date-fns; unread rows have bg-muted/40 background; "×" button to markRead(id)
      - Empty state: "No notifications yet" centered text when notifications.length === 0

    Severity badge colors:
    - urgent: variant="destructive"
    - headsup: variant="default"
    - digest: variant="secondary"
    - (use shadcn Badge component from @/components/ui/badge)

    Update `DashboardHeader.tsx`:
    - Add `onNavigate: (view: string) => void` to DashboardHeaderProps
    - Import and render `<NotificationBell userId={user?.id} onNavigate={onNavigate} />` between SidebarTrigger and LogOut button

    Update `Dashboard.tsx`:
    - Pass `onNavigate={setActiveView}` to `<DashboardHeader>` (DashboardHeader already receives user; add onNavigate prop)
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/ 2>&1 | tail -5</automated>
  </verify>
  <done>NotificationBell.tsx exists. DashboardHeader accepts onNavigate prop. Dashboard.tsx passes onNavigate={setActiveView}. No TypeScript errors (tsc --noEmit passes or vitest compile step passes).</done>
</task>

</tasks>

<verification>
Full suite green: `cd worrylesssuperagent && npx vitest run` exits 0. NotificationBell renders in DashboardHeader — visible between sidebar trigger and logout button.
</verification>

<success_criteria>
- useNotifications hook: initial count, realtime increment, markRead, markAllRead, resolveView all implemented and tested
- NotificationBell popover panel: badge, notification list, severity labels, mark-read controls, navigation on click
- DashboardHeader updated to accept onNavigate; Dashboard.tsx passes setActiveView
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/05-org-view-notifications/05-02-SUMMARY.md`
</output>
