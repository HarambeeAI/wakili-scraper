---
phase: 09-tech-debt-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - worrylesssuperagent/src/components/onboarding/ConversationalOnboarding.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "ConversationalOnboarding.tsx compiles with zero TypeScript errors after removal"
    - "The Step type no longer contains 'processing' or 'complete' union members"
    - "handleComplete function has zero occurrences in the codebase"
    - "No unused state variables remain from handleComplete (progress, statusMessage, isLoading, briefingProgress)"
    - "The full vitest suite continues to pass"
  artifacts:
    - path: "worrylesssuperagent/src/components/onboarding/ConversationalOnboarding.tsx"
      provides: "Onboarding flow without dead code"
      contains: "handleTeamAccept"
  key_links:
    - from: "ConversationalOnboarding.tsx"
      to: "handleTeamAccept"
      via: "terminal onboarding path"
      pattern: "handleTeamAccept"
---

<objective>
Remove the dead handleComplete() function and unreachable "processing" / "complete" Step union members from ConversationalOnboarding.tsx. These became dead code when Phase 2 refactored the terminal onboarding path to use handleTeamAccept() -> "briefing" -> "push_opt_in" -> onComplete(). The dead code is approximately 100 lines of unreachable branches and associated state variables.

Purpose: Eliminate maintenance risk from misleading dead code that looks like an alternative code path.
Output: Slimmed ConversationalOnboarding.tsx that compiles cleanly and passes the vitest suite.
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tech-debt-cleanup/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead handleComplete code and orphaned state variables</name>
  <files>worrylesssuperagent/src/components/onboarding/ConversationalOnboarding.tsx</files>
  <action>
Make the following surgical removals in order. After each removal, confirm the file still parses (the final TypeScript compile is the gate, not intermediate parses).

**Step 1 — Remove union members from the Step type (around line 54-55):**
Remove `| "processing"` and `| "complete"` from the Step discriminated union. The type becomes a union of the remaining valid steps only.

**Step 2 — Remove the handleComplete function body (lines 669–776 approx):**
Delete the entire `const handleComplete = async () => { ... }` function. Confirm zero remaining call sites with: `grep -n "handleComplete" worrylesssuperagent/src/components/onboarding/ConversationalOnboarding.tsx`
Expected result: zero matches (the grep should produce empty output).

**Step 3 — Remove case "processing": and case "complete": blocks from renderStep() (lines 1323–1355 approx):**
Delete both unreachable case blocks from the switch statement inside renderStep(). The switch should fall through to `default:` or the next valid case without these branches.

**Step 4 — Remove "processing" and "complete" entries from the step-number map (around lines 612–613):**
The map that computes `currentStepNumber` for the progress bar contains entries for these two dead steps. Remove those two entries. The remaining entries (welcome through push_opt_in) define the progress percentage.

**Step 5 — Fix the progress-bar guard (around line 1361):**
The guard currently reads: `step !== "welcome" && step !== "complete"`. Since "complete" is no longer in the union, TypeScript will flag this as always-true comparison. Update the guard to: `step !== "welcome" && step !== "push_opt_in"` — this hides the progress bar on the terminal push opt-in step, which is consistent with the intent of hiding the bar at the end of onboarding.

**Step 6 — Audit and remove orphaned useState variables:**
After removing handleComplete, grep for each of the following names to confirm they have zero remaining usages (excluding their own declaration):
- `progress` / `setProgress`
- `statusMessage` / `setStatusMessage`
- `briefingProgress` / `setBriefingProgress`
- `isLoading` / `setIsLoading` (only if exclusively used by handleComplete — check carefully; isLoading may be used elsewhere)

Remove the `useState` declaration for each variable confirmed to have zero usages. Do NOT remove variables that are referenced elsewhere in the component.

Note: The `Progress` import from `@/components/ui/progress` may still be used by the briefing step's progress bar (line 1304 area). Keep the import if any remaining reference exists. Remove it only if confirmed unused.

**Step 7 — Remove unused lucide-react imports:**
Check if `Check` or any other icon was imported exclusively for the `"complete"` case. Remove only icons confirmed unused. Do not remove icons that appear elsewhere in the JSX.
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx tsc --noEmit 2>&1 | head -30 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` exits 0 with no errors
    - `grep -c "handleComplete" src/components/onboarding/ConversationalOnboarding.tsx` returns 0
    - `grep -c '"processing"\|"complete"' src/components/onboarding/ConversationalOnboarding.tsx` returns 0 (or only in comments)
    - `npx vitest run` passes green
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `grep handleComplete worrylesssuperagent/src/components/onboarding/ConversationalOnboarding.tsx` produces empty output
- `npx vitest run` from worrylesssuperagent/ passes all tests
</verification>

<success_criteria>
ConversationalOnboarding.tsx contains no "processing" or "complete" Step values, no handleComplete function, no orphaned useState calls for progress/statusMessage/briefingProgress (unless still used), and the file compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tech-debt-cleanup/09-01-SUMMARY.md`
</output>
