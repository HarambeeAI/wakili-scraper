---
phase: 08-phase-verifications
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-heartbeat-system/04-VERIFICATION.md
autonomous: true
requirements:
  - HB-01
  - HB-02
  - HB-03
  - HB-04
  - HB-05
  - HB-06
  - HB-07
  - HB-08
  - HB-09
  - SEC-02

must_haves:
  truths:
    - "04-VERIFICATION.md exists with explicit PASS/FAIL for HB-01..09 and SEC-02"
    - "HB-01 and dispatcher camelCase fix from Phase 6 is confirmed in source and marked PASS (fixed)"
    - "HB-03 HEARTBEAT_OK suppression confirmed in runner source (no DB write on ok severity)"
    - "HB-05 budget enforcement confirmed in get_due_heartbeat_agents SQL or dispatcher query"
    - "SEC-02 service-role key usage confirmed in dispatcher (no JWT from caller)"
  artifacts:
    - path: ".planning/phases/04-heartbeat-system/04-VERIFICATION.md"
      provides: "Formal verification record for Phase 4 Heartbeat System"
      contains: "PASS/FAIL"
  key_links:
    - from: "supabase/functions/heartbeat-dispatcher/index.ts"
      to: "HB-01, HB-05, HB-06, SEC-02"
      via: "dispatcher logic — snake_case keys, budget, active hours, service role"
      pattern: "user_agent_id|user_id|agent_type_id"
    - from: "supabase/functions/heartbeat-runner/index.ts"
      to: "HB-02, HB-03, HB-04, HB-07"
      via: "runner severity routing and ok suppression"
      pattern: "severity.*ok|HEARTBEAT_OK"
    - from: "supabase/migrations/20260313000008_heartbeat_dispatcher_fn.sql"
      to: "HB-05, HB-06"
      via: "get_due_heartbeat_agents SQL function with budget + active hours"
      pattern: "heartbeat_daily_budget|AT TIME ZONE"
    - from: "src/components/agents/HeartbeatConfigSection.tsx"
      to: "HB-08"
      via: "interval/active-hours/toggle UI"
      pattern: "heartbeat_interval|active_hours"
---

<objective>
Produce the formal VERIFICATION.md for Phase 4 (Heartbeat System) by code-reviewing edge functions, migrations, the config UI component, and test files against the five Phase 4 success criteria and ten requirements (HB-01..09, SEC-02). This verification must account for Phase 6 bug fixes: the dispatcher camelCase→snake_case fix that makes HB-01..09 actually functional.

Purpose: Phase 4 shipped with a critical dispatcher field name bug that was fixed in Phase 6. The VERIFICATION.md records the current (fixed) state — not the original buggy state — and marks all HB requirements PASS (fixed) with evidence from the corrected source.
Output: `.planning/phases/04-heartbeat-system/04-VERIFICATION.md`
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-phase-verifications/08-RESEARCH.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Phase 4 source files and compile evidence</name>
  <files>
    worrylesssuperagent/supabase/functions/heartbeat-dispatcher/index.ts
    worrylesssuperagent/supabase/functions/heartbeat-runner/index.ts
    worrylesssuperagent/supabase/functions/send-morning-digest/index.ts
    worrylesssuperagent/supabase/migrations/20260313000006_heartbeat_queue.sql
    worrylesssuperagent/supabase/migrations/20260313000007_heartbeat_cron_jobs.sql
    worrylesssuperagent/supabase/migrations/20260313000008_heartbeat_dispatcher_fn.sql
    worrylesssuperagent/supabase/migrations/20260313000009_morning_digest_cron.sql
    worrylesssuperagent/src/hooks/useHeartbeatConfig.ts
  </files>
  <action>
    Read each file above. Also check for test files at these paths:
    - `worrylesssuperagent/src/__tests__/heartbeatParser.test.ts`
    - `worrylesssuperagent/src/__tests__/heartbeatDispatcher.test.ts`
    - `worrylesssuperagent/src/__tests__/useHeartbeatConfig.test.ts`
    And find the HeartbeatConfigSection component (check `src/components/agents/HeartbeatConfigSection.tsx` or similar).
    Also check `worrylesssuperagent/supabase/functions/_shared/heartbeatParser.ts` for the shared parser module.

    Evidence to collect per requirement:

    - HB-01: In heartbeat-dispatcher/index.ts, confirm: (a) single pg_cron trigger referenced in migration 00007, (b) message payload uses snake_case keys: user_agent_id, user_id, agent_type_id (NOT camelCase). This is the Phase 6 fix. Extract the exact lines. Mark HB-01 PASS (fixed Phase 6) with evidence.
    - HB-02: In heartbeat-runner/index.ts, confirm: reads up to 5 messages from heartbeat_jobs queue (pgmq.read call with limit 5). Confirm it reads HEARTBEAT.md from workspace (or buildWorkspacePrompt call).
    - HB-03: In heartbeat-runner/index.ts, confirm: when severity === "ok", the runner does NOT insert to agent_heartbeat_log and does NOT create a notification. Find the ok branch and confirm it is a no-op (or early return). This is the HEARTBEAT_OK suppression.
    - HB-04: Confirm severity routing: urgent → push + email + in-app notification row; headsup → in-app notification row only; digest → notification row with severity='digest' for morning digest batching. Check the notification INSERT and any Resend/push call sites.
    - HB-05: In migration 00008 (get_due_heartbeat_agents SQL function), confirm budget enforcement: daily calls counted, agents at budget limit excluded. Extract the SQL WHERE clause or COUNT condition.
    - HB-06: In migration 00008 (or dispatcher), confirm active hours check using profiles.timezone and AT TIME ZONE conversion. Confirm agents outside active_hours_start..active_hours_end are excluded.
    - HB-07: Confirm agent_heartbeat_log INSERT in runner includes: agent_type_id (or agent_type), user_id, severity (added in migration 00009), finding, timestamp (created_at). Check migration 00009 for the severity column ALTER TABLE.
    - HB-08: In HeartbeatConfigSection component and useHeartbeatConfig hook, confirm: interval selector (1h/2h/4h/8h options), active hours start/end time inputs, enabled toggle. Confirm changes persist to user_agents table.
    - HB-09: In send-morning-digest/index.ts, confirm it: queries digest-severity notifications from past 24 hours, consolidates into a briefing, delivers at user-timezone 8am (via next_digest_run_at column in migration 00011 or 00009). Check migration 00009 for next_digest_run_at pattern or similar scheduling column.
    - SEC-02: In heartbeat-dispatcher/index.ts, confirm: uses service role key (Deno.env.get service_role or SUPABASE_SERVICE_ROLE_KEY), NOT req.headers.get('Authorization'). Confirm user identity comes from user_agents table query, not from caller input.

    For each Phase 6 fix (HB-01 snake_case), note: "Bug: [original camelCase keys]. Fix: [current snake_case keys at line N]. Phase 6 Plan 01 closed this."
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>Evidence collected for all 10 requirements. Phase 6 dispatcher fix confirmed in source. Vitest suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-VERIFICATION.md</name>
  <files>.planning/phases/04-heartbeat-system/04-VERIFICATION.md</files>
  <action>
    Write the VERIFICATION.md at `.planning/phases/04-heartbeat-system/04-VERIFICATION.md`.

    **Frontmatter:**
    ```yaml
    ---
    phase: 04-heartbeat-system
    verified_by: Claude (Phase 8 automated code review)
    verified_at: [today's date]
    overall_status: passed
    requirements_verified: [HB-01, HB-02, HB-03, HB-04, HB-05, HB-06, HB-07, HB-08, HB-09, SEC-02]
    gap_closures_accounted:
      - phase: 06
        fix: dispatcher camelCase→snake_case field names
        requirements_affected: [HB-01, HB-02, HB-03, HB-04, HB-05, HB-06, HB-07, HB-08, HB-09]
    ---
    ```

    **Sections:**

    1. `## Phase 4 Success Criteria` — 5 subsections matching ROADMAP.md Phase 4 criteria verbatim:
       - SC-1: pg_cron dispatcher queries due agents with active-hours + budget filtering
       - SC-2: severity ok → zero DB writes
       - SC-3: severity urgent → notification + push + email in same invocation
       - SC-4: Agent settings shows heartbeat config section (interval, active hours, toggle)
       - SC-5: Daily call budget (default 6) enforced by dispatcher
       Each with: Status, Evidence (file + line), Notes.

    2. `## Requirements Map` — Table: Requirement | Status | Evidence | Notes
       For HB-01..09, SEC-02.
       For HB-01..09, include gap-closure accounting entry:
       "PASS (fixed Phase 6) — Bug: dispatcher used camelCase keys {userAgentId, userId, agentTypeId}; runner expected snake_case. Fix confirmed at heartbeat-dispatcher/index.ts line N."

    3. `## Integration Points` — Note: dispatcher→runner pgmq message contract (snake_case keys confirmed). runner→notifications→realtime chain. runner→Resend email. dispatcher→get_due_heartbeat_agents SQL function.

    4. `## Manual Verification Required` — pg_cron job execution (requires live DB), end-to-end heartbeat flow with pgmq, push notification delivery (requires deployed VAPID keys). Include specific test instructions for each.

    5. `## Sign-Off` block.

    Rules:
    - PASS requires file+line evidence
    - Use "PASS (fixed Phase 6)" format for HB-01..09 to make gap-closure visible
    - Never mark FAIL for items that have confirmed source code fixes
    - SEC-02: confirm service role pattern explicitly — this requirement cannot use a manual test; it must be confirmed in source
  </action>
  <verify>
    <automated>ls /Users/anthonysure/Documents/Worryless\ AI\ 33/.planning/phases/04-heartbeat-system/04-VERIFICATION.md && grep -c "PASS\|FAIL\|MANUAL" /Users/anthonysure/Documents/Worryless\ AI\ 33/.planning/phases/04-heartbeat-system/04-VERIFICATION.md</automated>
  </verify>
  <done>04-VERIFICATION.md exists. Contains PASS/FAIL/MANUAL for all 10 requirements. HB-01..09 marked PASS (fixed Phase 6) with dispatcher fix evidence. SEC-02 service-role confirmed in source. Sign-off block present.</done>
</task>

</tasks>

<verification>
1. `.planning/phases/04-heartbeat-system/04-VERIFICATION.md` exists
2. `grep "HB-01\|HB-02\|HB-03\|HB-04\|HB-05\|HB-06\|HB-07\|HB-08\|HB-09\|SEC-02" .planning/phases/04-heartbeat-system/04-VERIFICATION.md` returns 10+ lines
3. `cd worrylesssuperagent && npx vitest run` — 51 passing, 0 failed
</verification>

<success_criteria>
- 04-VERIFICATION.md exists with correct YAML frontmatter including gap_closures_accounted
- Explicit PASS/FAIL/MANUAL for all 10 requirements
- HB-01..09 show "PASS (fixed Phase 6)" with dispatcher snake_case key evidence
- HB-03 ok-suppression confirmed with specific runner line reference
- SEC-02 confirmed from dispatcher source (no JWT from caller)
- Vitest suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/08-phase-verifications/08-03-SUMMARY.md`
</output>
