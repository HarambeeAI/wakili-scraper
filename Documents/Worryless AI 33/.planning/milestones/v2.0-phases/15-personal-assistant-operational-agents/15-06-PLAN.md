---
phase: 15-personal-assistant-operational-agents
plan: 06
type: execute
wave: 3
depends_on: ["15-02", "15-03", "15-04", "15-05"]
files_modified:
  - worrylesssuperagent/langgraph-server/src/agents/personal-assistant.ts
  - worrylesssuperagent/langgraph-server/src/agents/personal-assistant.test.ts
  - worrylesssuperagent/langgraph-server/src/agents/customer-support.ts
  - worrylesssuperagent/langgraph-server/src/agents/legal-compliance.ts
  - worrylesssuperagent/langgraph-server/src/agents/hr.ts
  - worrylesssuperagent/langgraph-server/src/agents/pr-comms.ts
  - worrylesssuperagent/langgraph-server/src/agents/procurement.ts
  - worrylesssuperagent/langgraph-server/src/agents/data-analyst.ts
  - worrylesssuperagent/langgraph-server/src/agents/operations.ts
  - worrylesssuperagent/langgraph-server/src/agents/ops-classification.test.ts
autonomous: true
requirements: [PA-01, PA-02, PA-03, PA-04, PA-05, PA-06, PA-07, PA-08, PA-09, PA-10, OPS-01, OPS-02, OPS-03, OPS-04, OPS-05, OPS-06, OPS-07]

must_haves:
  truths:
    - "All 8 agent graphs use tool-wired topology: readMemory -> {agent}Tools -> llmNode -> writeMemory -> respond"
    - "Request classification uses regex heuristics (no LLM call for routing)"
    - "Tool results injected into state.businessContext.{agent}ToolResults"
    - "System prompts updated to reflect real tool access (no 'no tools yet' disclaimer)"
    - "All classification tests pass"
  artifacts:
    - path: "worrylesssuperagent/langgraph-server/src/agents/personal-assistant.ts"
      provides: "PA graph with paTools node + classifyPARequest"
      exports: ["createPersonalAssistantGraph", "classifyPARequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/customer-support.ts"
      provides: "CS graph with csTools node + classifyCSRequest"
      exports: ["createCustomerSupportGraph", "classifyCSRequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/legal-compliance.ts"
      provides: "Legal graph with legalTools node + classifyLegalRequest"
      exports: ["createLegalComplianceGraph", "classifyLegalRequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/hr.ts"
      provides: "HR graph with hrTools node + classifyHRRequest"
      exports: ["createHRGraph", "classifyHRRequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/pr-comms.ts"
      provides: "PR graph with prTools node + classifyPRRequest"
      exports: ["createPRCommsGraph", "classifyPRRequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/procurement.ts"
      provides: "Procurement graph with procurementTools node + classifyProcurementRequest"
      exports: ["createProcurementGraph", "classifyProcurementRequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/data-analyst.ts"
      provides: "DA graph with daTools node + classifyDARequest"
      exports: ["createDataAnalystGraph", "classifyDARequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/operations.ts"
      provides: "Ops graph with opsTools node + classifyOpsRequest"
      exports: ["createOperationsGraph", "classifyOpsRequest"]
    - path: "worrylesssuperagent/langgraph-server/src/agents/personal-assistant.test.ts"
      provides: "PA classification tests"
    - path: "worrylesssuperagent/langgraph-server/src/agents/ops-classification.test.ts"
      provides: "Classification tests for all 7 operational agents"
  key_links:
    - from: "src/agents/personal-assistant.ts"
      to: "src/tools/pa/index.ts"
      via: "import tools for dispatch in paTools node"
      pattern: "from.*tools/pa/index"
    - from: "src/agents/customer-support.ts"
      to: "src/tools/customer-support/index.ts"
      via: "import tools for csTools node"
      pattern: "from.*tools/customer-support/index"
    - from: "src/agents/data-analyst.ts"
      to: "src/tools/data-analyst/index.ts"
      via: "import tools for daTools node"
      pattern: "from.*tools/data-analyst/index"
---

<objective>
Rewrite all 8 agent graphs from simple createBaseAgentGraph wrappers to tool-wired topology with classification + tools nodes. Add request classification tests for PA and all 7 operational agents.

Purpose: Wires all tools built in Plans 02-05 into their agent graphs so every agent can actually execute real tool operations, not just converse.
Output: 8 rewritten agent files + 2 test files.
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-personal-assistant-operational-agents/15-RESEARCH.md
@.planning/phases/15-personal-assistant-operational-agents/15-02-SUMMARY.md
@.planning/phases/15-personal-assistant-operational-agents/15-03-SUMMARY.md
@.planning/phases/15-personal-assistant-operational-agents/15-04-SUMMARY.md
@.planning/phases/15-personal-assistant-operational-agents/15-05-SUMMARY.md

<interfaces>
From src/agents/accountant.ts (reference implementation for graph rewrite):
```typescript
// Graph topology: __start__ -> readMemory -> accountantTools -> llmNode -> writeMemory -> respond
export function classifyAccountantRequest(content: string): AccountantClassification { /* regex */ }
export function createAccountantToolsNode() { /* returns async state handler */ }
export function createAccountantGraph(checkpointer?: PostgresSaver) { /* StateGraph build */ }
```

From src/agents/marketer.ts (reference implementation):
```typescript
export function classifyMarketerRequest(content: string): MarketerClassification { /* regex */ }
export function createMarketerToolsNode() { /* returns async state handler */ }
export function createMarketerGraph(checkpointer?: PostgresSaver) { /* StateGraph build */ }
```

From src/agents/base-agent.ts:
```typescript
export function createLLMNode(config: BaseAgentConfig): (state) => Promise<StateUpdate>;
export function createRespondNode(): (state) => Promise<Command>;
export interface BaseAgentConfig { agentType: AgentTypeId; systemPrompt: string; }
```

From src/memory/read-memory.ts + write-memory.ts:
```typescript
export function createReadMemoryNode(): (state) => Promise<StateUpdate>;
export function createWriteMemoryNode(): (state) => Promise<StateUpdate>;
```

From src/types/agent-state.ts:
```typescript
// AgentState.State includes: messages, userId, businessContext, memoryContext, responseMetadata
// businessContext is Record<string, unknown> -- tools inject as keyed entries
```

Classification type contracts (from Plan 01):
- PAClassification: isReadEmails, isTriageInbox, isDraftEmail, isSendEmail, isListCalendar, isCreateEvent, isMeetingBrief, isSearchDrive, isDetectConflicts, isTimeAllocation
- CSClassification: isCreateTicket, isListTickets, isUpdateTicket, isSearchKB, isHealthScore, isChurnDetection
- LegalClassification: isReviewContract, isListContracts, isDraftTemplate, isContractCalendar, isMonitorRegulatory
- HRClassification: isCreateJobPosting, isScreenResume, isTrackCandidate, isListCandidates, isOnboardingPlan, isPerformanceReview
- PRClassification: isDraftPressRelease, isMonitorMedia, isTrackCoverage, isAnalyzeSentiment
- ProcClassification: isSearchSuppliers, isCompareQuotes, isCreatePO, isScoreVendor
- DAClassification: isCrossFunctionalQuery, isStatisticalAnalysis, isAnomalyDetection, isGenerateChart, isKPIAggregation
- OpsClassification: isCreateProject, isTrackMilestones, isListProjects, isAnalyzeBottlenecks, isDraftSOP
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PA graph rewrite + classification test</name>
  <files>
    worrylesssuperagent/langgraph-server/src/agents/personal-assistant.ts,
    worrylesssuperagent/langgraph-server/src/agents/personal-assistant.test.ts
  </files>
  <read_first>
    worrylesssuperagent/langgraph-server/src/agents/personal-assistant.ts,
    worrylesssuperagent/langgraph-server/src/agents/accountant.ts,
    worrylesssuperagent/langgraph-server/src/agents/marketer.ts,
    worrylesssuperagent/langgraph-server/src/agents/base-agent.ts,
    worrylesssuperagent/langgraph-server/src/types/agent-state.ts,
    worrylesssuperagent/langgraph-server/src/tools/pa/index.ts,
    worrylesssuperagent/langgraph-server/src/agents/marketer.test.ts
  </read_first>
  <action>
Fully rewrite `src/agents/personal-assistant.ts` following the accountant.ts/marketer.ts pattern. Replace the `createBaseAgentGraph` wrapper with custom 5-node graph.

The file has 3 sections:

**Section 1: System Prompt**
Keep the existing PERSONAL_ASSISTANT_SYSTEM_PROMPT role description but:
- Remove the line "You do NOT have tool access yet -- respond conversationally based on what the user asks. Tool execution will be added in a future update."
- Add:
```
You have access to real Google Workspace tools. When you have tool results in your context, use them to provide precise, actionable responses.

Available tools:
- Email: read inbox, triage by urgency/topic, draft responses matching your style, send emails (requires your approval)
- Calendar: list events, create events with availability check (requires your approval), detect conflicts, analyze time allocation
- Meeting prep: synthesize attendee info, email history, and Drive documents into a pre-meeting brief
- Drive: search files by name or content
```

**Section 2: Request Classification**
Export `classifyPARequest(content: string): PAClassification` with regex patterns:

```typescript
export function classifyPARequest(content: string): PAClassification {
  return {
    isReadEmails: /\b(read|check|show|get|fetch).*(email|inbox|mail|messages)\b/i.test(content),
    isTriageInbox: /\b(triage|categorize|prioritize|sort|organize).*(inbox|email|mail)\b/i.test(content),
    isDraftEmail: /\b(draft|write|compose|reply|respond).*(email|mail|response|message)\b/i.test(content),
    isSendEmail: /\b(send|deliver|forward).*(email|mail|message|response)\b/i.test(content),
    isListCalendar: /\b(calendar|schedule|events|meetings|agenda|what.*today|what.*week)\b/i.test(content),
    isCreateEvent: /\b(create|schedule|book|set up|add).*(event|meeting|appointment|call)\b/i.test(content),
    isMeetingBrief: /\b(brief|prep|prepare|summary).*(meeting|call|event)\b/i.test(content),
    isSearchDrive: /\b(search|find|look for|locate).*(file|document|doc|drive|folder)\b/i.test(content),
    isDetectConflicts: /\b(conflict|overlap|double.?book|clash|schedule.*issue)\b/i.test(content),
    isTimeAllocation: /\b(time.*alloc|meeting.*time|focus.*time|how.*much.*time|time.*spent|busiest)\b/i.test(content),
  };
}
```

**Section 3: PA Tools Node + Graph Factory**

Create `createPAToolsNode()` dispatching tools based on classification:
- `isReadEmails` -> call `readEmails(state.userId)` (data-gathering, safe to run)
- `isTriageInbox` -> call `triageInbox(state.userId)` (data-gathering)
- `isDraftEmail` -> set `needsInput: { requestType: "draftEmail", message: "Need email ID and optional instructions" }`
- `isSendEmail` -> set `needsInput: { requestType: "sendEmail", message: "Need drafted email details" }`
- `isListCalendar` -> call `listCalendarEvents(state.userId)` (data-gathering)
- `isCreateEvent` -> set `needsInput: { requestType: "createEvent", message: "Need event details (summary, start, end)" }`
- `isMeetingBrief` -> set `needsInput: { requestType: "meetingBrief", message: "Need event ID for meeting brief" }`
- `isSearchDrive` -> set `needsInput: { requestType: "searchDrive", message: "Need search query" }`
- `isDetectConflicts` -> call `detectCalendarConflicts(state.userId)` (data-gathering)
- `isTimeAllocation` -> call `analyzeTimeAllocation(state.userId)` (data-gathering)

All tool results go to `state.businessContext.paToolResults`.

Create `createPersonalAssistantGraph(checkpointer?)` with topology:
```
readMemory -> paTools -> llmNode -> writeMemory -> respond
```

Import all from `"../tools/pa/index.js"`. Use `StateGraph(AgentState)` with 5 nodes, same as accountant/marketer pattern. Cast compile options.

Then create `src/agents/personal-assistant.test.ts`:

```typescript
import { describe, it, expect } from "vitest";
import { classifyPARequest } from "./personal-assistant.js";

describe("classifyPARequest", () => {
  it("detects read emails intent", () => {
    expect(classifyPARequest("Check my email inbox").isReadEmails).toBe(true);
  });
  it("detects triage inbox intent", () => {
    expect(classifyPARequest("Triage my inbox by urgency").isTriageInbox).toBe(true);
  });
  it("detects draft email intent", () => {
    expect(classifyPARequest("Draft a reply to that email").isDraftEmail).toBe(true);
  });
  it("detects send email intent", () => {
    expect(classifyPARequest("Send that email now").isSendEmail).toBe(true);
  });
  it("detects list calendar intent", () => {
    expect(classifyPARequest("What meetings do I have today?").isListCalendar).toBe(true);
  });
  it("detects create event intent", () => {
    expect(classifyPARequest("Schedule a meeting with John at 2pm").isCreateEvent).toBe(true);
  });
  it("detects meeting brief intent", () => {
    expect(classifyPARequest("Prepare a brief for my 3pm meeting").isMeetingBrief).toBe(true);
  });
  it("detects search drive intent", () => {
    expect(classifyPARequest("Find the project proposal document").isSearchDrive).toBe(true);
  });
  it("detects conflict detection intent", () => {
    expect(classifyPARequest("Do I have any schedule conflicts?").isDetectConflicts).toBe(true);
  });
  it("detects time allocation intent", () => {
    expect(classifyPARequest("How much time have I spent in meetings this week?").isTimeAllocation).toBe(true);
  });
  it("returns all false for unrelated input", () => {
    const cls = classifyPARequest("Hello, how are you?");
    expect(Object.values(cls).every((v) => v === false)).toBe(true);
  });
});
```
  </action>
  <verify>
    <automated>cd worrylesssuperagent/langgraph-server && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run src/agents/personal-assistant.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/agents/personal-assistant.ts contains "export function classifyPARequest"
    - src/agents/personal-assistant.ts contains "createPAToolsNode"
    - src/agents/personal-assistant.ts contains "export function createPersonalAssistantGraph"
    - src/agents/personal-assistant.ts contains "paToolResults"
    - src/agents/personal-assistant.ts contains ".addNode(\"paTools\""
    - src/agents/personal-assistant.ts contains ".addEdge(\"readMemory\", \"paTools\")"
    - src/agents/personal-assistant.ts contains ".addEdge(\"paTools\", \"llmNode\")"
    - src/agents/personal-assistant.ts does NOT contain "You do NOT have tool access yet"
    - src/agents/personal-assistant.ts contains "Available tools:"
    - src/agents/personal-assistant.ts contains import from "../tools/pa/index.js"
    - personal-assistant.test.ts passes all tests
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>PA graph rewritten to tool-wired topology. classifyPARequest uses 10 regex patterns. All classification tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CS, Legal, HR, PR agent graph rewrites + partial classification tests</name>
  <files>
    worrylesssuperagent/langgraph-server/src/agents/customer-support.ts,
    worrylesssuperagent/langgraph-server/src/agents/legal-compliance.ts,
    worrylesssuperagent/langgraph-server/src/agents/hr.ts,
    worrylesssuperagent/langgraph-server/src/agents/pr-comms.ts
  </files>
  <read_first>
    worrylesssuperagent/langgraph-server/src/agents/customer-support.ts,
    worrylesssuperagent/langgraph-server/src/agents/legal-compliance.ts,
    worrylesssuperagent/langgraph-server/src/agents/hr.ts,
    worrylesssuperagent/langgraph-server/src/agents/pr-comms.ts,
    worrylesssuperagent/langgraph-server/src/agents/accountant.ts,
    worrylesssuperagent/langgraph-server/src/agents/marketer.ts,
    worrylesssuperagent/langgraph-server/src/agents/personal-assistant.ts,
    worrylesssuperagent/langgraph-server/src/tools/customer-support/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/legal/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/hr/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/pr/index.ts
  </read_first>
  <action>
Rewrite 4 agent files. Each follows the IDENTICAL pattern: keep existing system prompt (remove "You do NOT have tool access yet" line, add tool-aware instructions), add regex classifier, add tools node, build 5-node graph.

**CUSTOMER SUPPORT (`customer-support.ts`):**
- Import from `"../tools/customer-support/index.js"`
- `classifyCSRequest` regex patterns:
  - `isCreateTicket`: /\b(create|open|new|submit|file).*(ticket|issue|case|request)\b/i
  - `isListTickets`: /\b(list|show|get|view|open).*(ticket|issue|case|queue)\b/i
  - `isUpdateTicket`: /\b(update|close|resolve|change.*status).*(ticket|issue|case)\b/i
  - `isSearchKB`: /\b(search|find|look up|knowledge|help|how to|documentation|faq)\b/i
  - `isHealthScore`: /\b(health|score|satisfaction|customer.*health)\b/i
  - `isChurnDetection`: /\b(churn|at.?risk|losing|retention|leaving)\b/i
- Tools node: `isListTickets` -> call `listTickets(state.userId)`, `isSearchKB` -> needsInput, `isChurnDetection` -> call `detectChurnRisk(state.userId)`, others needsInput
- Results key: `businessContext.csToolResults`

**LEGAL (`legal-compliance.ts`):**
- Import from `"../tools/legal/index.js"`
- `classifyLegalRequest` regex patterns:
  - `isReviewContract`: /\b(review|analyze|check|assess).*(contract|agreement|terms)\b/i
  - `isListContracts`: /\b(list|show|all|active).*(contract|agreement)\b/i
  - `isDraftTemplate`: /\b(draft|create|template|write).*(nda|msa|sow|contract|agreement)\b/i
  - `isContractCalendar`: /\b(renewal|expir|upcoming|calendar|deadline).*(contract|agreement)?\b/i
  - `isMonitorRegulatory`: /\b(regulat|compliance|legal.*change|law.*update|policy)\b/i
- Tools node: `isListContracts` -> call `listContracts(state.userId)`, `isContractCalendar` -> call `contractCalendar(state.userId)`, others needsInput
- Results key: `businessContext.legalToolResults`

**HR (`hr.ts`):**
- Import from `"../tools/hr/index.js"`
- `classifyHRRequest` regex patterns:
  - `isCreateJobPosting`: /\b(create|write|draft|post).*(job|position|role|opening)\b/i
  - `isScreenResume`: /\b(screen|review|evaluate|assess).*(resume|cv|candidate|applicant)\b/i
  - `isTrackCandidate`: /\b(add|track|new).*(candidate|applicant)\b/i
  - `isListCandidates`: /\b(list|show|all|pipeline).*(candidate|applicant|position)\b/i
  - `isOnboardingPlan`: /\b(onboard|30.?60.?90|new.*hire|welcome)\b/i
  - `isPerformanceReview`: /\b(performance|review|feedback|evaluation|appraisal)\b/i
- Tools node: `isListCandidates` -> call `listCandidates(state.userId)`, others needsInput
- Results key: `businessContext.hrToolResults`

**PR (`pr-comms.ts`):**
- Import from `"../tools/pr/index.js"`
- `classifyPRRequest` regex patterns:
  - `isDraftPressRelease`: /\b(draft|write|create).*(press|release|announcement)\b/i
  - `isMonitorMedia`: /\b(monitor|track|scan|mention|media|coverage|press)\b/i
  - `isTrackCoverage`: /\b(log|record|add|track).*(coverage|article|press|mention)\b/i
  - `isAnalyzeSentiment`: /\b(sentiment|perception|how.*look|public.*opinion|brand.*image)\b/i
- Tools node: `isAnalyzeSentiment` -> call `analyzeSentiment(state.userId)`, others needsInput
- Results key: `businessContext.prToolResults`

**For ALL 4 agents:** Follow the exact createAccountantGraph/createMarketerGraph pattern:
```typescript
const graph = new StateGraph(AgentState)
  .addNode("readMemory", createReadMemoryNode())
  .addNode("{agent}Tools", create{Agent}ToolsNode())
  .addNode("llmNode", createLLMNode(config))
  .addNode("writeMemory", createWriteMemoryNode())
  .addNode("respond", createRespondNode(), { ends: [] })
  .addEdge("__start__", "readMemory")
  .addEdge("readMemory", "{agent}Tools")
  .addEdge("{agent}Tools", "llmNode")
  .addEdge("llmNode", "writeMemory")
  .addEdge("writeMemory", "respond");
```
  </action>
  <verify>
    <automated>cd worrylesssuperagent/langgraph-server && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/agents/customer-support.ts contains "export function classifyCSRequest"
    - src/agents/customer-support.ts contains ".addNode(\"csTools\""
    - src/agents/customer-support.ts contains "csToolResults"
    - src/agents/customer-support.ts does NOT contain "You do NOT have tool access yet"
    - src/agents/legal-compliance.ts contains "export function classifyLegalRequest"
    - src/agents/legal-compliance.ts contains ".addNode(\"legalTools\""
    - src/agents/legal-compliance.ts contains "legalToolResults"
    - src/agents/hr.ts contains "export function classifyHRRequest"
    - src/agents/hr.ts contains ".addNode(\"hrTools\""
    - src/agents/hr.ts contains "hrToolResults"
    - src/agents/pr-comms.ts contains "export function classifyPRRequest"
    - src/agents/pr-comms.ts contains ".addNode(\"prTools\""
    - src/agents/pr-comms.ts contains "prToolResults"
    - NONE of the 4 agent files contain "You do NOT have tool access yet"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CS, Legal, HR, PR agent graphs rewritten to tool-wired topology. Each has regex classifier + tools node + 5-node graph. All "no tools yet" disclaimers removed.</done>
</task>

<task type="auto">
  <name>Task 3: Procurement, Data Analyst, Operations agent graph rewrites + all 7 ops classification tests</name>
  <files>
    worrylesssuperagent/langgraph-server/src/agents/procurement.ts,
    worrylesssuperagent/langgraph-server/src/agents/data-analyst.ts,
    worrylesssuperagent/langgraph-server/src/agents/operations.ts,
    worrylesssuperagent/langgraph-server/src/agents/ops-classification.test.ts
  </files>
  <read_first>
    worrylesssuperagent/langgraph-server/src/agents/procurement.ts,
    worrylesssuperagent/langgraph-server/src/agents/data-analyst.ts,
    worrylesssuperagent/langgraph-server/src/agents/operations.ts,
    worrylesssuperagent/langgraph-server/src/agents/accountant.ts,
    worrylesssuperagent/langgraph-server/src/agents/personal-assistant.ts,
    worrylesssuperagent/langgraph-server/src/agents/marketer.test.ts,
    worrylesssuperagent/langgraph-server/src/tools/procurement/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/data-analyst/index.ts,
    worrylesssuperagent/langgraph-server/src/tools/operations/index.ts
  </read_first>
  <action>
Rewrite 3 remaining agent files, then create the full ops classification test file covering all 7 operational agents.

**PROCUREMENT (`procurement.ts`):**
- Import from `"../tools/procurement/index.js"`
- `classifyProcurementRequest` regex patterns:
  - `isSearchSuppliers`: /\b(find|search|discover|source).*(supplier|vendor|provider)\b/i
  - `isCompareQuotes`: /\b(compare|quote|bid|price.*comparison|side.*by.*side)\b/i
  - `isCreatePO`: /\b(create|place|submit|order|purchase.*order|buy|procure)\b/i
  - `isScoreVendor`: /\b(score|rate|evaluate|rank|assess).*(vendor|supplier)\b/i
- Tools node: all needsInput (require user-provided parameters)
- Results key: `businessContext.procurementToolResults`

**DATA ANALYST (`data-analyst.ts`):**
- Import from `"../tools/data-analyst/index.js"`
- `classifyDARequest` regex patterns:
  - `isCrossFunctionalQuery`: /\b(query|data|report|show me|what.*is|how.*many|total|revenue|expense|leads|invoice|ticket|project)\b/i
  - `isStatisticalAnalysis`: /\b(statistic|correlation|regression|mean|median|average|distribution)\b/i
  - `isAnomalyDetection`: /\b(anomal|outlier|unusual|spike|unexpected|abnormal)\b/i
  - `isGenerateChart`: /\b(chart|graph|visual|plot|diagram|bar.*chart|line.*chart|pie.*chart)\b/i
  - `isKPIAggregation`: /\b(kpi|metric|dashboard|key.*indicator|performance.*indicator|overview|summary)\b/i
- Tools node: `isKPIAggregation` -> call `kpiAggregation(state.userId)`, `isCrossFunctionalQuery` -> needsInput (needs queryType), others needsInput
- Results key: `businessContext.daToolResults`

**OPERATIONS (`operations.ts`):**
- Import from `"../tools/operations/index.js"`
- `classifyOpsRequest` regex patterns:
  - `isCreateProject`: /\b(create|start|new|launch).*(project|initiative|program)\b/i
  - `isTrackMilestones`: /\b(milestone|progress|status|track|update).*(project|milestone)?\b/i
  - `isListProjects`: /\b(list|show|all|active).*(project|initiative)\b/i
  - `isAnalyzeBottlenecks`: /\b(bottleneck|blocked|stuck|delay|impediment|blocker)\b/i
  - `isDraftSOP`: /\b(sop|standard.*operat|procedure|process.*document|playbook)\b/i
- Tools node: `isListProjects` -> call `listProjects(state.userId)`, `isAnalyzeBottlenecks` -> call `analyzeBottlenecks(state.userId)`, others needsInput
- Results key: `businessContext.opsToolResults`

**For ALL 3 agents:** Follow the exact 5-node graph pattern (same as Task 2).

Then create `src/agents/ops-classification.test.ts` -- a single test file covering all 7 operational agents:

```typescript
import { describe, it, expect } from "vitest";
import { classifyCSRequest } from "./customer-support.js";
import { classifyLegalRequest } from "./legal-compliance.js";
import { classifyHRRequest } from "./hr.js";
import { classifyPRRequest } from "./pr-comms.js";
import { classifyProcurementRequest } from "./procurement.js";
import { classifyDARequest } from "./data-analyst.js";
import { classifyOpsRequest } from "./operations.js";

describe("Customer Support classification", () => {
  it("detects create ticket", () => expect(classifyCSRequest("Open a new support ticket").isCreateTicket).toBe(true));
  it("detects KB search", () => expect(classifyCSRequest("Search the knowledge base for return policy").isSearchKB).toBe(true));
  it("detects churn", () => expect(classifyCSRequest("Which customers are at risk of churning?").isChurnDetection).toBe(true));
  it("returns false for unrelated", () => expect(Object.values(classifyCSRequest("Hello")).every(v => v === false)).toBe(true));
});

describe("Legal classification", () => {
  it("detects contract review", () => expect(classifyLegalRequest("Review this contract for risks").isReviewContract).toBe(true));
  it("detects renewals", () => expect(classifyLegalRequest("What contracts are expiring soon?").isContractCalendar).toBe(true));
  it("detects template draft", () => expect(classifyLegalRequest("Draft an NDA template").isDraftTemplate).toBe(true));
});

describe("HR classification", () => {
  it("detects job posting", () => expect(classifyHRRequest("Create a job posting for engineer").isCreateJobPosting).toBe(true));
  it("detects resume screening", () => expect(classifyHRRequest("Screen this resume for the PM role").isScreenResume).toBe(true));
  it("detects onboarding", () => expect(classifyHRRequest("Create a 30-60-90 onboarding plan").isOnboardingPlan).toBe(true));
});

describe("PR classification", () => {
  it("detects press release", () => expect(classifyPRRequest("Draft a press release for our launch").isDraftPressRelease).toBe(true));
  it("detects media monitoring", () => expect(classifyPRRequest("Monitor media mentions of our brand").isMonitorMedia).toBe(true));
  it("detects sentiment", () => expect(classifyPRRequest("How is our brand perception?").isAnalyzeSentiment).toBe(true));
});

describe("Procurement classification", () => {
  it("detects supplier search", () => expect(classifyProcurementRequest("Find suppliers for office furniture").isSearchSuppliers).toBe(true));
  it("detects PO creation", () => expect(classifyProcurementRequest("Create a purchase order for 50 laptops").isCreatePO).toBe(true));
  it("detects vendor scoring", () => expect(classifyProcurementRequest("Score our top vendor").isScoreVendor).toBe(true));
});

describe("Data Analyst classification", () => {
  it("detects query intent", () => expect(classifyDARequest("Show me revenue by month").isCrossFunctionalQuery).toBe(true));
  it("detects anomaly", () => expect(classifyDARequest("Are there any anomalies in our data?").isAnomalyDetection).toBe(true));
  it("detects chart", () => expect(classifyDARequest("Generate a bar chart of expenses").isGenerateChart).toBe(true));
  it("detects KPI", () => expect(classifyDARequest("Show me the KPI dashboard").isKPIAggregation).toBe(true));
});

describe("Operations classification", () => {
  it("detects project creation", () => expect(classifyOpsRequest("Create a new project for Q3 launch").isCreateProject).toBe(true));
  it("detects bottleneck analysis", () => expect(classifyOpsRequest("Which milestones are blocked?").isAnalyzeBottlenecks).toBe(true));
  it("detects SOP drafting", () => expect(classifyOpsRequest("Draft a standard operating procedure for onboarding").isDraftSOP).toBe(true));
  it("detects list projects", () => expect(classifyOpsRequest("Show all active projects").isListProjects).toBe(true));
});
```
  </action>
  <verify>
    <automated>cd worrylesssuperagent/langgraph-server && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run src/agents/ops-classification.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/agents/procurement.ts contains "export function classifyProcurementRequest"
    - src/agents/procurement.ts contains ".addNode(\"procurementTools\""
    - src/agents/procurement.ts contains "procurementToolResults"
    - src/agents/data-analyst.ts contains "export function classifyDARequest"
    - src/agents/data-analyst.ts contains ".addNode(\"daTools\""
    - src/agents/data-analyst.ts contains "daToolResults"
    - src/agents/operations.ts contains "export function classifyOpsRequest"
    - src/agents/operations.ts contains ".addNode(\"opsTools\""
    - src/agents/operations.ts contains "opsToolResults"
    - NONE of the 3 agent files contain "You do NOT have tool access yet"
    - ops-classification.test.ts passes all tests
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Procurement, Data Analyst, Operations agent graphs rewritten. All 7 ops classification tests pass. Full vitest suite passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vitest run src/agents/personal-assistant.test.ts src/agents/ops-classification.test.ts --reporter=verbose` passes all classification tests
- All 8 agent files use tool-wired topology: readMemory -> {agent}Tools -> llmNode -> writeMemory -> respond
- No agent file contains "You do NOT have tool access yet"
- All agents import from their respective tools/index.ts barrel
- Full test suite: `npx vitest run --reporter=verbose` passes
</verification>

<success_criteria>
- All 8 agents have tool-wired graph topology (not createBaseAgentGraph wrappers)
- Regex classification for all 8 agents (no LLM cost for routing)
- Tool results injected into businessContext.{agent}ToolResults
- System prompts reflect real tool capabilities
- All classification tests pass
- Full vitest suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-personal-assistant-operational-agents/15-06-SUMMARY.md`
</output>
