---
phase: 05-org-view-notifications
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - worrylesssuperagent/src/__tests__/useNotifications.test.ts
  - worrylesssuperagent/src/__tests__/useTeamData.test.ts
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-05, NOTIF-06, ORG-02, ORG-03]

must_haves:
  truths:
    - "All vitest tests in src/__tests__/ exit 0 after Wave 0"
    - "useNotifications.test.ts has it.todo stubs covering initial count, realtime increment, markRead, markAllRead, navigation"
    - "useTeamData.test.ts has it.todo stubs covering team data fetch and getHeartbeatStatus pure function"
  artifacts:
    - path: "worrylesssuperagent/src/__tests__/useNotifications.test.ts"
      provides: "Wave 0 test stubs for notification bell (NOTIF-01, NOTIF-02, NOTIF-05, NOTIF-06)"
    - path: "worrylesssuperagent/src/__tests__/useTeamData.test.ts"
      provides: "Wave 0 test stubs for org chart data + getHeartbeatStatus (ORG-02, ORG-03)"
  key_links:
    - from: "useNotifications.test.ts"
      to: "src/hooks/useNotifications.ts"
      via: "it.todo stubs become real tests in Wave 1"
      pattern: "it.todo"
    - from: "useTeamData.test.ts"
      to: "src/hooks/useTeamData.ts"
      via: "it.todo stubs + getHeartbeatStatus pure function test"
      pattern: "it.todo"
---

<objective>
Create Wave 0 test scaffolds for Phase 5 hooks so the Nyquist rule is satisfied before any implementation starts.

Purpose: Plans 02 and 03 (Wave 1) add real test implementations into these files. Without the files existing, Wave 1 verify commands would fail on a missing file error rather than a real test result.
Output: Two test files with it.todo stubs and one runnable pure-function test (getHeartbeatStatus).
</objective>

<execution_context>
@/Users/anthonysure/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonysure/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-org-view-notifications/05-RESEARCH.md
@.planning/phases/05-org-view-notifications/05-VALIDATION.md

<!-- Pattern reference: existing test stubs follow Phase 4 it.todo pattern -->
@worrylesssuperagent/src/__tests__/useHeartbeatConfig.test.ts
@worrylesssuperagent/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useNotifications test scaffold</name>
  <files>worrylesssuperagent/src/__tests__/useNotifications.test.ts</files>
  <action>
    Create `worrylesssuperagent/src/__tests__/useNotifications.test.ts` with it.todo stubs matching the VALIDATION.md test map. Follow the same pattern as `useHeartbeatConfig.test.ts` (it.todo stubs, describe block, no imports from non-existent hooks yet).

    Required stubs:
    - describe("useNotifications") with:
      - it.todo("initial count: returns unread count from DB on mount")
      - it.todo("realtime: increments unread count on INSERT postgres_changes event")
      - it.todo("markRead: decrements unread count optimistically and calls supabase update")
      - it.todo("markAllRead: sets unread count to 0 optimistically and calls supabase update")
      - it.todo("navigation: resolves LEGACY_VIEW_MAP agent type IDs to correct view strings")

    No real imports, no vi.mock() calls — it.todo stubs only so vitest exits 0.
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/useNotifications.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>File exists, vitest reports 5 todo tests, suite exits 0</done>
</task>

<task type="auto">
  <name>Task 2: useTeamData test scaffold + getHeartbeatStatus pure test</name>
  <files>worrylesssuperagent/src/__tests__/useTeamData.test.ts</files>
  <action>
    Create `worrylesssuperagent/src/__tests__/useTeamData.test.ts`.

    This file has TWO sections:
    1. A runnable describe("getHeartbeatStatus") with real test cases (pure function, no mocking needed — the function will live in `src/lib/heartbeatStatus.ts` and is importable once Plan 03 creates it). For Wave 0, keep these as it.todo stubs since the function doesn't exist yet:
       - it.todo("status: returns 'sleeping' when lastRunAt is null")
       - it.todo("status: returns 'active' when lastRunAt is within the last hour and outcome is not surfaced")
       - it.todo("status: returns 'attention' when lastOutcome is 'surfaced'")
       - it.todo("status: returns 'sleeping' when lastRunAt is more than 1 hour ago")

    2. A describe("useTeamData") with it.todo stubs:
       - it.todo("team data: returns active agents with display_name from available_agent_types")
       - it.todo("team data: separates chief_of_staff into top-tier vs other agents")

    No imports, no vi.mock() — it.todo only. vitest must exit 0.
  </action>
  <verify>
    <automated>cd /Users/anthonysure/Documents/Worryless\ AI\ 33/worrylesssuperagent && npx vitest run src/__tests__/useTeamData.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>File exists, vitest reports 6 todo tests, suite exits 0</done>
</task>

</tasks>

<verification>
Both test files exist. Full test suite passes: `cd worrylesssuperagent && npx vitest run` exits 0 (all existing tests remain green, new todo stubs add no failures).
</verification>

<success_criteria>
- `src/__tests__/useNotifications.test.ts` exists with 5 it.todo stubs
- `src/__tests__/useTeamData.test.ts` exists with 6 it.todo stubs
- `npx vitest run` exits 0 with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-org-view-notifications/05-01-SUMMARY.md`
</output>
